# Development Notes

## Codex usage experiment

PacePrompt records Codex model-usage counters for each Codex-assisted commit. This is an engineering-process experiment modelled on WeeklyHealthReport's development notes. The figures measure repeated model processing across a task, not source-code size, unique conversation text, developer productivity or human effort.

The API-equivalent value is a comparison estimate only. This work runs through a ChatGPT subscription and is not billed as API usage. Token pricing does not include any separately priced API tools or external services unless a row explicitly says otherwise.

## Recording protocol

1. Assign the change a stable ID in the form `PP-YYYYMMDD-NN`.
2. Record the task's exact cumulative usage snapshot before implementation begins.
3. Immediately before the commit, record another snapshot after code, tests, review and this note are complete.
4. Subtract the start snapshot from the end snapshot. If no valid start snapshot exists, label the result as a task aggregate rather than a per-commit delta.
5. Add one row to the table below and include `PacePrompt-Change: <change-id>` as a commit-message trailer. The trailer maps the row to the resulting commit without attempting to place a commit's own hash inside itself.
6. Record the exact model, official pricing URL and pricing date used. Recheck pricing whenever the model or published rate changes.

Record these counters separately:

- total input tokens;
- cached input tokens, which are included within total input;
- cache-write input tokens, when reported;
- output tokens, including any reasoning tokens already counted within output;
- total tokens.

Do not reconstruct missing counters from source size, elapsed time or subscription usage. Mark missing measurements as `Unmeasured`.

## API-equivalent calculation

For GPT-5.6 Sol, the official promotional rates checked on 2 September 2026 and rechecked on 3 September 2026 are $4.00 per million uncached input tokens, $0.40 per million cached input tokens and $20.00 per million output tokens. Cache writes are billed at 1.25 times the uncached-input rate. Prompts above the model's published long-context threshold require request-level calculation rather than applying a single aggregate rate.

For a phase with no cache-write or long-context adjustment:

```text
uncached input = total input - cached input
API-equivalent =
    (uncached input / 1,000,000 × input rate)
  + (cached input / 1,000,000 × cached-input rate)
  + (output / 1,000,000 × output rate)
```

Pricing authority: [OpenAI GPT-5.6 Sol model documentation](https://developers.openai.com/api/docs/models/gpt-5.6-sol).

For Claude Fable 5.1 (`claude-fable-5-1`), the official rates checked on 4 September 2026 are $10.00 per million uncached input tokens, $0.25 per million cache-read input tokens, $12.50 per million five-minute-TTL cache-write input tokens (1.25 times the input rate), $20.00 per million one-hour-TTL cache-write input tokens (2 times the input rate) and $50.00 per million output tokens. Claude Code sessions in this repository use the one-hour cache TTL, and the session JSONL reports cache writes per TTL, so each cache write is priced at its TTL-specific rate. Claude Code counters come from the session file under `~/.claude/projects/<project>/<session-id>.jsonl`: assistant records are deduplicated by `message.id` because one API response is written as several lines that repeat the same usage; `input_tokens` is the uncached input; total input is uncached input plus cache reads plus cache writes; thinking tokens are already a subset of output.

For a Claude Code phase with TTL-specific cache writes:

```text
API-equivalent =
    (uncached input / 1,000,000 × input rate)
  + (cache-read input / 1,000,000 × cache-read rate)
  + (five-minute cache-write input / 1,000,000 × five-minute write rate)
  + (one-hour cache-write input / 1,000,000 × one-hour write rate)
  + (output / 1,000,000 × output rate)
```

Pricing authority for Claude models: [Anthropic pricing documentation](https://platform.claude.com/docs/en/pricing).

## Commit measurements

| Date | Change ID | Feature or change | Model | Input tokens (cached; cache write) | Output tokens | Total tokens | Token-only API-equivalent | Measurement scope |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| 2 Sep 2026 | `PP-20260902-01` | Repository baseline and non-motion FTMS capability explorer | `gpt-5.6-sol` | 22,180,485 (21,742,208; 0) | 100,109 | 22,280,594 | $12.45 | Initial task aggregate |
| 2 Sep 2026 | `PP-20260902-02` | Issue #1 read-only FTMS live telemetry and diagnostic capture | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Exact start/end task counters and model identifier unavailable |
| 2 Sep 2026 | `PP-20260902-03` | Issue #2 safe FR30z FTMS Control Point handshake specification | `gpt-5.6-sol` | 12,396,013 (11,889,664; 0) | 50,316 | 12,446,329 | $7.79 | Task aggregate through pre-commit snapshot |
| 3 Sep 2026 | `PP-20260903-01` | Training Status initial read and diagnostic source provenance | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Exact start/end task counters and model identifier unavailable |
| 3 Sep 2026 | `PP-20260903-02` | Correct issue #2 Codex usage accounting | `gpt-5.6-sol` | 2,075,901 (1,902,464; 0) | 7,522 | 2,083,423 | $1.61 | Exact phase delta through final counter snapshot |
| 3 Sep 2026 | `PP-20260903-03` | Issue #3 deterministic workout-plan schema and validator | `gpt-5.6-sol` | 10,697,748 (10,567,552; 0) | 35,945 | 10,733,693 | $5.47 | Exact implementation-phase delta through final pre-commit snapshot |
| 3 Sep 2026 | `PP-20260903-04` | Issue #8 reproducible Codex phase-accounting tooling | `gpt-5.6-sol` | 9,519,742 (9,277,952; 0) | 73,706 | 9,593,448 | $6.15 | Exact implementation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-05` | Issue #4 safety-gated workout execution state-machine design | `gpt-5.6-sol` | 8,516,525 (8,130,432; 0) | 43,885 | 8,560,410 | $5.67 | Exact documentation-phase delta through final pre-commit ledger snapshot |
| 3 Sep 2026 | `PP-20260903-06` | Issue #9 2c-ceiling app icon installation | `gpt-5.6-sol` | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Exact start captured; final report rejected a malformed repeated cumulative snapshot |
| 3 Sep 2026 | `PP-20260903-07` | Issue #5 local workout storage and history contract | `gpt-5.6-sol` | 5,208,336 (5,086,080; 0) | 22,129 | 5,230,465 | $2.97 | Exact documentation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-08` | Issue #10 versioned local saved-plan repository | `gpt-5.6-sol` | 5,673,858 (5,510,400; 0) | 38,502 | 5,712,360 | $3.63 | Exact implementation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-09` | Issue #11 manual workout creation, validation, preview and save | `gpt-5.6-sol` | 20,585,493 (20,350,080; 0) | 61,971 | 20,647,464 | $10.32 | Exact implementation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-10` | Issue #6 provider-neutral workout proposal, privacy and evaluation contract | `gpt-5.6-sol` | 4,792,941 (4,671,872; 0) | 17,495 | 4,810,436 | $2.70 | Exact documentation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-11` | Issue #12 provider-neutral workout-import evaluation corpus and deterministic scorer | `gpt-5.6-sol` | 11,664,765 (11,423,872; 0) | 67,908 | 11,732,673 | $6.89 | Exact implementation-phase delta through final pre-ledger snapshot |
| 3 Sep 2026 | `PP-20260903-12` | Issue #13 developer-only workout-import evaluation target and provider adapters | `gpt-5.6-sol` | 16,088,427 (15,753,344; 0) | 83,713 | 16,172,140 | $9.32 | Exact implementation-phase delta through final pre-ledger snapshot |
| 4 Sep 2026 | `PP-20260904-01` | Issue #15 host-side prompt-v2 evaluation harness and measured OpenRouter evaluation | `gpt-5.6-sol` | 112,965,849 (111,188,352; 0) | 486,356 | 113,452,205 | $61.31 | Whole-session aggregate through final pre-ledger snapshot |
| 5 Sep 2026 | `PP-20260904-02` | Issue #15 ratified workout-import prompt v3 | `claude-fable-5-1` | 1,714,863 (1,602,944; 111,519) | 35,153 | 1,750,016 | $4.39 | Exact session delta from the first user message through the recorded phase-end assistant message |
| 5 Sep 2026 | `PP-20260905-01` | Issue #15 ratified workout-import v3 corpus and non-duplication review | `claude-fable-5-1` | 4,510,506 (4,371,319; 138,763) | 94,606 | 4,605,112 | $8.60 | Exact phase delta from the corpus-authorisation message through the recorded phase-end assistant message |
| 5 Sep 2026 | `PP-20260905-02` | Issue #15 v3 corpus correction: H064 rewritten as a purely recursive sequence | `claude-fable-5-1` | 325,969 (325,757; 210) | 3,529 | 329,498 | $0.26 | Exact phase delta from the H064 correction message through the recorded phase-end assistant message |
| 5 Sep 2026 | `PP-20260905-03` | Issue #15 v3 corpus revision: six held-out cases restructured, H055 step kinds, review few-shot flag assertion | `claude-fable-5-1` | 1,764,769 (1,743,820; 20,879) | 16,449 | 1,781,218 | $1.68 | Exact phase delta from the operator's restructuring approval through the recorded phase-end assistant message |
| 5 Sep 2026 | `PP-20260905-04` | Issue #15 confirmatory prompt-v3 host evaluation harness and operator gate | `gpt-5.6-sol` | 44,766,463 (43,667,072; 0) | 196,060 | 44,962,523 | $25.79 | Exact phase delta through final pre-ledger snapshot |
| 5 Sep 2026 | `PP-20260905-05` | Issue #15 merge-based prompt-v3 corpus and host-harness integration | `gpt-5.6-sol` | 669,688 (604,032; 0) | 2,216 | 671,904 | $0.55 | Exact integration-phase delta through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-01` | Issue #17 off-device open-weight workout-import evaluation and ratified no-candidate decision | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Exact phase baseline rejected an unchanged cumulative snapshot with non-zero metered request counters |
| 6 Sep 2026 | `PP-20260906-02` | Consolidated model-evaluation report, aggregate data and four charts | `gpt-6-astra` | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Original session failed cumulative-counter validation; no estimate substituted |
| 6 Sep 2026 | `PP-20260906-03` | Classification diagnostics, confusion matrices and deterministic evaluation methodology | `gpt-6-astra` | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Baseline and final capture rejected the earlier session counter reset |
| 6 Sep 2026 | `PP-20260906-04` | Issue #19 consent-gated production workout import and exact mocked request verification | `gpt-6-astra`; `gpt-5.6-sol` | See separate phase measurements below | See separate phase measurements below | See separate phase measurements below | $15.24 + $1.53 + $2.94 + $0.13 | Four exact, non-overlapping phase deltas retained separately; no blended token total |
| 6 Sep 2026 | `PP-20260906-05` | Fresh-install saved-plan storage repair and simulator verification | `gpt-5.6-sol` | 10,906,326 (10,752,640; 0) | 23,307 | 10,929,633 | $5.38 | Exact manually calculated implementation phase through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-06` | Issue #19 redacted live-response identity diagnostics | `gpt-5.6-sol` | 13,158,105 (12,955,520; 0) | 21,811 | 13,179,916 | $6.43 | Exact manually calculated diagnostic phase through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-07` | Issue #19 redacted top-level model-identity classification | `gpt-5.6-sol` | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Baseline capture rejected the earlier session counter reset; no estimate substituted |
| 6 Sep 2026 | `PP-20260906-08` | Issue #19 provider-bound requested-model alias acceptance | `gpt-5.6-sol` | Unmeasured | Unmeasured | Unmeasured | Unmeasured | Baseline and final capture rejected the earlier session counter reset; no estimate substituted |
| 6 Sep 2026 | `PP-20260906-09` | Post-reset Codex accounting recovery for the issue #19/#26 handover | `gpt-5.6-sol` | 448,019 (427,904; 0) | 1,528 | 449,547 | $0.28 | Exact manually verified post-reset phase delta through token event 943 |
| 6 Sep 2026 | `PP-20260906-10` | Issue #26 DEBUG-only redacted import-validation diagnostics | `gpt-5.6-sol` | 9,564,468 (9,367,424; 0) | 27,377 | 9,591,845 | $5.08 | Exact implementation-phase delta through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-11` | Issue #19 documented OpenRouter usage-envelope compatibility | `gpt-5.6-sol` | 4,097,296 (3,984,768; 0) | 7,190 | 4,104,486 | $2.19 | Exact implementation-phase delta through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-12` | Issue #19 remaining OpenRouter response-envelope compatibility | `gpt-5.6-sol` | 5,299,611 (5,212,800; 0) | 10,165 | 5,309,776 | $2.64 | Exact implementation-phase delta through final pre-ledger snapshot |
| 6 Sep 2026 | `PP-20260906-13` | Issue #29 README reconciliation for production workout import | `gpt-5.6-sol` | 1,495,568 (1,483,136; 0) | 3,159 | 1,498,727 | $0.71 | Exact documentation-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-14` | Issue #31 confirmed saved-plan deletion | `gpt-5.6-sol` | 8,369,828 (8,202,240; 0) | 19,741 | 8,389,569 | $4.35 | Exact manually verified post-reset implementation-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-15` | Correct issue #31 Codex usage accounting | `gpt-5.6-sol` | 788,762 (777,088; 0) | 3,071 | 791,833 | $0.42 | Exact manually verified post-reset correction-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-16` | Post-reset bounded phase accounting support | `gpt-5.6-sol` | 2,823,320 (2,784,896; 0) | 11,360 | 2,834,680 | $1.49 | Exact implementation-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-17` | Issue #35 previewable selected saved-plan JSON export | `gpt-5.6-sol` | 14,356,523 (14,005,120; 0) | 40,382 | 14,396,905 | $7.82 | Exact implementation-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-18` | Issue #37 versioned workout-execution summary repository | `gpt-5.6-sol` | 6,595,528 (6,408,704; 0) | 32,869 | 6,628,397 | $3.97 | Exact implementation-phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-19` | Curated current-screen and future Watch design reference | `gpt-5.6-sol` | 1,828,341 (1,804,800; 0) | 4,733 | 1,833,074 | $0.91 | Exact post-reset artifact-copy phase delta through final pre-ledger snapshot |
| 7 Sep 2026 | `PP-20260907-20` | Issue #40 design-aligned evidence-safe Home entry screen | `gpt-5.6-sol` | 9,568,318 (9,136,768; 0) | 41,644 | 9,609,962 | $6.21 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-01` | Issue #42 design-aligned fail-closed Plans library | `gpt-5.6-sol` | 16,035,725 (15,849,088; 0) | 26,973 | 16,062,698 | $7.63 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-02` | Issue #44 design-aligned manual plan editor and exact review | `gpt-5.6-sol` | 19,510,616 (19,068,160; 0) | 43,851 | 19,554,467 | $10.27 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-03` | Deterministic CI and informational Codecov production-coverage baseline | `gpt-5.6-sol` | 10,881,866 (10,707,200; 0) | 24,243 | 10,906,109 | $5.47 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-04` | Isolate the import-timeout request-count assertion after the first hosted CI run | `gpt-5.6-sol` | 3,620,157 (3,513,088; 0) | 7,603 | 3,627,760 | $1.99 | Exact corrective implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-05` | Run the complete production simulator suite on one deterministic destination | `gpt-5.6-sol` | 3,685,277 (3,646,720; 0) | 3,170 | 3,688,447 | $1.68 | Exact corrective implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-06` | Issue #49 pure safety-gated workout execution reducer | `gpt-5.6-sol` | 27,184,624 (26,836,352; 0) | 87,265 | 27,271,889 | $13.87 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-07` | Issue #67 Plans XCUITest condition-driven synchronisation | `gpt-5.6-sol` | 12,473,585 (12,192,128; 0) | 21,133 | 12,494,718 | $6.43 | Exact implementation-phase delta through final pre-ledger snapshot |
| 8 Sep 2026 | `PP-20260908-08` | Issue #69 temporary local-first complete validation policy | `gpt-5.6-sol` | 3,339,448 (3,257,984; 0) | 10,015 | 3,349,463 | $1.83 | Exact implementation-phase delta through final pre-ledger snapshot |
| 9 Sep 2026 | `PP-20260909-01` | Issue #50 FTMS Control Point codec and single-procedure transport | `gpt-5.6-sol` | 8,883,375 (8,568,576; 0) | 44,674 | 8,928,049 | $5.58 | Exact implementation-phase delta through final pre-ledger snapshot |
| 9 Sep 2026 | `PP-20260909-02` | Issue #50 parser-decoded range round-trip repair | `gpt-5.6-sol` | 4,286,388 (4,257,408; 0) | 4,928 | 4,291,316 | $1.92 | Exact independent-review repair-phase delta through final pre-ledger snapshot |
| 9 Sep 2026 | `PP-20260909-03` | Issue #51 supervised Request Control diagnostic, failed proof and reset-spanning accounting repair | `gpt-5.6-sol` | 31,484,181 (30,630,016; 0) | 81,432 | 31,565,613 | $17.30 | Exact reset-spanning implementation and physical-proof phase through final pre-ledger snapshot |
| 10 Sep 2026 | `PP-20260910-01` | Issue #72 callback-order-safe FTMS Control Point evidence correlation | `gpt-5.6-sol` | 5,195,993 (5,025,024; 0) | 14,534 | 5,210,527 | $2.98 | Exact implementation-phase delta through final pre-ledger snapshot |
| 10 Sep 2026 | `PP-20260910-02` | Issue #51 authorised reproof preparation and pre-trigger process abort | `gpt-5.6-sol` | 6,729,070 (6,415,744; 0) | 12,197 | 6,741,267 | $4.06 | Exact preparation and abort-record phase delta through final pre-ledger snapshot |

`PP-20260910-02` records a second operator-authorised issue #51 preparation from exact `origin/main` `c4fa7bb85bcf4e858bdc587a46ccaa6b83542122`, including the issue #72 callback-order repair. The installed app still held the consumed first-attempt one-shot flag, so a temporary uncommitted v2 gate key was used to preserve unrelated app data while retaining the same fail-closed exact-`00` allow-list and consume-before-forward behaviour. All 67 focused codec, transport, diagnostic and presentation tests passed, followed by a signed Debug iPhone build and in-place installation. The operator reported that the identified FR30z reached **Ready for one request**, but the app process then terminated with signal 9 before the final confirmations or trigger. The raw console capture is too sparse to independently prove the final link state or absence of traffic. The operator explicitly confirmed disconnection, that no Request Control was sent and that no treadmill movement occurred. The physical-session authority ended at that abort: there was no relaunch, reconnect, compensating command, `0x2AD9` write, ATT result, indication or procedure outcome. A signed Release build, with the DEBUG-only diagnostic compiled out, was installed without launching it or deleting app data; the temporary gate change and test were reverted and are not in this candidate. Issue #51 remains open and tracker #48 is unchanged.

The final documentation-only candidate passed `scripts/validate_local.sh`: the sealed 20-case corpus, 30 scorer tests, four summary tests, 82 host-evaluation tests with two documented optional sibling-worktree skips, all 238 production unit tests and 23 UI tests with zero failures or skips, production LCOV export without a threshold, the unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests, all 24 accounting-helper tests and `git diff --check`. Validation used an iPhone 17 Pro simulator running iOS 26.5. Simulator and deterministic-test results are not FR30z Request Control evidence.

The selected root Codex Desktop session is `01a084fa-1c03-74f3-958a-ccec0e04b0ba`, file `/Users/sertanyamaner/.codex/sessions/2026/09/09/rollout-2026-09-09T08-02-58-01a084fa-1c03-74f3-958a-ccec0e04b0ba.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 698 at `2026-09-10T07:53:21.726Z`: 8,292,424 input tokens, of which 7,961,984 were cached, 0 cache-write input tokens, and 30,709 output tokens, including 11,655 reasoning tokens, for 8,323,133 total tokens. The final pre-ledger snapshot is cumulative token event 739 at `2026-09-10T09:01:17.862Z`: 15,021,494 input tokens, of which 14,377,728 were cached, 0 cache-write input tokens, and 42,906 output tokens, including 17,007 reasoning tokens, for 15,064,400 total tokens. Subtracting the baseline produced 6,729,070 input tokens, of which 6,415,744 were cached, 0 cache-write input tokens, and 12,197 output tokens, including 5,352 reasoning tokens, for 6,741,267 total tokens. All 39 intervening requests reconcile exactly; one exact replay at event 714 and one unchanged non-usage snapshot at event 736 are reported and excluded, no counter reset occurred inside the boundary, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 231,090 tokens. With 313,326 uncached input tokens, the token-only API-equivalent comparison estimate is `(313,326 x $4.00 + 6,415,744 x $0.40 + 12,197 x $20.00) / 1,000,000 = $4.0635416`, rounded to $4.06, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. Actual provider spend was $0.00. The phase includes the supervised physical Bluetooth connection only through operator-reported readiness; it includes no FTMS Control Point write, treadmill operation or movement, retry, reconnection, workout execution, provider call, HealthKit, Watch, dependency or signing repository change.

`PP-20260910-01` makes the single-procedure FTMS Control Point transport insensitive to the relative order of CoreBluetooth's indication and ATT write-result callbacks without weakening its evidence boundary. A well-formed response that matches the active connection epoch, procedure identifier and request opcode may be retained as provisional evidence when it arrives first, but it cannot complete the procedure or grant Control permission until the same procedure receives explicit ATT write acceptance. ATT rejection, delivery uncertainty, disconnect, malformed or mismatched bytes, a duplicate or late response, invalid monotonic time, a stale epoch or a different procedure all continue to fail closed. The 30-second indication deadline remains anchored to ATT acceptance; when a provisional response already exists, the recorded deadline is retained as evidence and no timer or second procedure is started. There is no retry, reconnection, resume, background behaviour, reducer/UI/orchestration integration or additional Control Point surface.

The adopted Bluetooth SIG Fitness Machine Service 1.0.1 and Fitness Machine Profile 1.0.1 revisions, both dated 1 October 2024, were reverified from the current official catalogue before the transport was changed. Apple's current documentation for the write-result and subscribed value-update delegate callbacks was also rechecked; it documents them independently and does not specify a relative ordering. Deterministic fake-link coverage passed all 38 codec, transport and Request Control diagnostic tests, including exact request bytes, early indication plus ATT accept/reject/unknown/disconnect, the exact 30-second deadline, malformed, mismatched, duplicate and late responses, invalid time, connection epochs and procedure identity. The complete local validation policy then passed the sealed 20-case corpus, 30 scorer tests, four summary tests, 82 host-evaluation tests with two documented optional sibling-worktree skips, all 238 production unit tests and 23 UI tests with zero failures or skips, production LCOV export without a threshold, the unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests, all 24 accounting-helper tests and `git diff --check`. This is simulator and deterministic-fake evidence only, not FR30z evidence.

The selected root Codex Desktop session is `01a084fa-1c03-74f3-958a-ccec0e04b0ba`, file `/Users/sertanyamaner/.codex/sessions/2026/09/09/rollout-2026-09-09T08-02-58-01a084fa-1c03-74f3-958a-ccec0e04b0ba.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 641 at `2026-09-10T07:27:18.255Z`: 770,597 input tokens, of which 682,112 were cached, 0 cache-write input tokens, and 4,011 output tokens, including 1,936 reasoning tokens, for 774,608 total tokens. The final pre-ledger snapshot is cumulative token event 674 at `2026-09-10T07:44:38.875Z`: 5,966,590 input tokens, of which 5,707,136 were cached, 0 cache-write input tokens, and 18,545 output tokens, including 7,399 reasoning tokens, for 5,985,135 total tokens. Subtracting the baseline produced 5,195,993 input tokens, of which 5,025,024 were cached, 0 cache-write input tokens, and 14,534 output tokens, including 5,463 reasoning tokens, for 5,210,527 total tokens. All 32 intervening requests reconcile exactly; one unchanged non-usage snapshot at event 670 is reported and excluded, no replay or counter reset occurred inside the boundary, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 233,059 tokens. With 170,969 uncached input tokens, the token-only API-equivalent comparison estimate is `(170,969 x $4.00 + 5,025,024 x $0.40 + 14,534 x $20.00) / 1,000,000 = $2.9845656`, rounded to $2.98, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge, issue closure and tracker maintenance are excluded. No provider request or external spend occurred. This software-only phase did not connect to a Bluetooth peripheral, send real FTMS Control Point traffic, access or operate a treadmill, or perform workout execution, HealthKit, Watch, dependency or signing work.

`PP-20260909-03` adds only a DEBUG-build, operator-supervised Request Control diagnostic for issue #51. It requires current decoded Fitness Machine Feature, speed-range and inclination-range evidence; completed passive subscription outcomes; confirmed FTMS Control Point indications; three explicit operator readiness confirmations; and a second destructive-action confirmation. A persistent one-shot gate consumes authority before forwarding and admits only the exact Request Control byte `00`; release builds compile the diagnostic path out. The diagnostic is not connected to the workout reducer, production orchestration or a general FTMS command surface, and it has no automatic retry, reconnect, resume or background behaviour. Deterministic focused coverage passed all 53 codec, transport, diagnostic-gate and presentation tests. Before installation, the complete local validation policy passed all 233 unit tests and 23 UI tests with zero failures or skips, production coverage export without a threshold, the unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests, all offline host and scorer checks, 23 then-current accounting-helper tests and `git diff --check` using Xcode 26.6 build 17F113 and an iPhone 17 Pro simulator running iOS 26.5. A Debug device build also succeeded with ignored local signing configuration; no signing repository change was made.

With the operator present at the identified FR30z, the deck clear, the belt stationary and immediate console and safety-key access confirmed, the app performed exactly one explicitly triggered Request Control procedure. It submitted exact request `00` once in connection epoch 1 with local procedure sequence 1. The treadmill returned well-formed success indication `80 00 01`, but CoreBluetooth delivered that indication callback before its successful ATT write callback. The reviewed transport correctly treated this as a protocol anomaly rather than success. The operator observed no movement and explicitly disconnected; CoreBluetooth confirmed the disconnect, the app was terminated, and the consumed one-shot gate prevents another write. This is a failed/unknown proof, not accepted Request Control evidence: issue #51 remains open, tracker #48 is unchanged, and target speed, target inclination, Start, Stop and Pause/Resume remain unresolved. No retry, reconnection, other FTMS Control Point command, treadmill operation, workout execution, provider request, HealthKit or Watch action occurred. The committed validation note is sanitised; the raw log containing device identifiers remains outside Git.

The same change hardens the repository-local accounting helper for a cumulative counter reset inside an exact phase. Instead of subtracting across a discontinuity, the helper now sums complete request records only after independently reconciling every monotonic cumulative epoch and requiring each reset event's new cumulative counters to equal its complete request counters field-for-field. It reports accepted in-boundary reset evidence and still fails closed on a missing or mismatched anchor. Regression coverage now comprises 24 helper tests, including exact delta and whole-session reset-spanning cases plus a corrupt-anchor rejection, and the bundled repository skill validator passed with pinned development-only PyYAML 6.0.3 in an isolated temporary environment. The final complete candidate gate then re-passed the 82 host tests with two documented optional sibling-worktree skips, sealed 20-case corpus, 30 scorer tests, four summary tests, all 233 production unit tests and 23 UI tests with zero failures or skips, production coverage export without a threshold, unsigned Release build, static analysis, all 14 developer-only evaluation tests, all 24 accounting-helper tests and `git diff --check`.

The selected root Codex Desktop session is `01a084fa-1c03-74f3-958a-ccec0e04b0ba`, file `/Users/sertanyamaner/.codex/sessions/2026/09/09/rollout-2026-09-09T08-02-58-01a084fa-1c03-74f3-958a-ccec0e04b0ba.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 239 at `2026-09-09T17:28:12.990Z`: 324,222 input tokens, of which 230,272 were cached, 0 cache-write input tokens, and 1,785 output tokens, including 957 reasoning tokens, for 326,007 total tokens. The final pre-ledger snapshot is cumulative token event 464 at `2026-09-09T19:48:38.874Z`: 12,313,691 input tokens, of which 11,800,832 were cached, 0 cache-write input tokens, and 36,499 output tokens, including 12,582 reasoning tokens, for 12,350,190 total tokens in the current cumulative epoch. The phase crosses reset edge 366->367 at `2026-09-09T19:05:43.204Z`; event 367 establishes an exact new epoch because both its cumulative and request counters are 217,193 input tokens, including 17,920 cached and 0 cache-write input tokens, plus 283 output tokens, including 47 reasoning tokens, for 217,476 total tokens. The helper independently reconciled all 221 measured requests on both sides of the reset and summed 31,484,181 input tokens, of which 30,630,016 were cached, 0 cache-write input tokens, and 81,432 output tokens, including 33,023 reasoning tokens, for 31,565,613 total tokens. Three unchanged non-usage snapshots at events 283, 376 and 444 and one exact replay at event 320 were reported and excluded. No request crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 233,124 tokens. With 854,165 uncached input tokens, the token-only API-equivalent comparison estimate is `(854,165 x $4.00 + 30,630,016 x $0.40 + 81,432 x $20.00) / 1,000,000 = $17.2973064`, rounded to $17.30, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. The ledger insertion, subsequent checks, commit, push, pull request, hosted review and GitHub maintenance are excluded. Actual provider spend was $0.00. The measured phase includes the supervised physical Bluetooth session and the single real Request Control `00` write described above; it includes no treadmill motion or operation, other FTMS command, retry, reconnection, workout execution, production integration, provider call, HealthKit, Watch, dependency or signing repository change.

`PP-20260909-02` repairs the exact-head independent-review finding that valid FTMS range values decoded from integer fields can carry normal binary floating-point tails and were therefore rejected by the initial codec's decimal-string equality check. The codec now reverses the protocol scale and accepts only values within two ULPs of the nearest raw integer, preserving rejection of genuinely off-grid, overflowing, non-finite, out-of-range and increment-misaligned inputs. Parser-to-codec regression fixtures verify exact round trips for raw speed `1999` exposed as `19.990000000000002 km/h` and raw signed inclination `-299` exposed as `-29.900000000000002%`. The focused codec and transport suite passed all 27 tests. The complete local validation policy passed the sealed 20-case corpus, 30 scorer tests, four summary tests, 82 host-evaluation tests with two expected sibling-worktree skips, all 247 production simulator tests with zero failures or skips, the LCOV export, unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests and all 23 accounting-helper tests. No scope boundary or production integration changed.

The selected root Codex Desktop session is `01a084fa-1c03-74f3-958a-ccec0e04b0ba`, file `/Users/sertanyamaner/.codex/sessions/2026/09/09/rollout-2026-09-09T08-02-58-01a084fa-1c03-74f3-958a-ccec0e04b0ba.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact repair baseline is cumulative token event 121 at `2026-09-09T08:02:55.065Z`: 16,713,452 input tokens, of which 16,298,368 were cached, 0 cache-write input tokens, and 57,864 output tokens, including 18,161 reasoning tokens, for 16,771,316 total tokens. The final pre-ledger snapshot is cumulative token event 142 at `2026-09-09T08:14:14.036Z`: 20,999,840 input tokens, of which 20,555,776 were cached, 0 cache-write input tokens, and 62,792 output tokens, including 19,940 reasoning tokens, for 21,062,632 total tokens. Subtracting the baseline produced 4,286,388 input tokens, of which 4,257,408 were cached, 0 cache-write input tokens, and 4,928 output tokens, including 1,779 reasoning tokens, for 4,291,316 total tokens. All 21 intervening requests reconcile exactly; no repeated snapshot, replay or counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 210,600 tokens. With 28,980 uncached input tokens, the token-only API-equivalent comparison estimate is `(28,980 x $4.00 + 4,257,408 x $0.40 + 4,928 x $20.00) / 1,000,000 = $1.9174432`, rounded to $1.92, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session. The independent review subagent, hosted validation, this numeric insertion, subsequent checks, commit, push, pull-request update, merge, issue closure and tracker maintenance are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, real FTMS Control Point write, workout execution, HealthKit, watchOS, Pause/Resume surface, signing or dependency change occurred.

`PP-20260909-01` implements only the bounded software delivery in issue #50. It adds an FTMS Control Point codec for Request Control, Set Target Speed, Set Target Inclination, Start and Stop; validates current decoded feature and range evidence without rounding or clamping; and decodes only exact three-byte responses for that reviewed surface. A separate single-procedure transport enables indications before any write, uses CoreBluetooth Write With Response, preserves one local procedure identifier and connection epoch, and records exact request bytes, monotonic submission time, ATT outcome, matching indication and response receipt time separately. It requires successful Request Control before any other scoped procedure, enforces the 30-second indication deadline from ATT write acceptance, and fails closed on unsupported properties, invalid values, malformed, reserved, mismatched, duplicate, out-of-order or late responses, delivery uncertainty, disconnects and invalid time. Timeout or delivery uncertainty invalidates the link and requires a separately established newer connection epoch; there is no automatic retry, reconnection, resume or background behaviour. The concrete CoreBluetooth link is deliberately not connected to the workout reducer, production UI or orchestration. The adopted Bluetooth SIG Fitness Machine Service 1.0.1 and Fitness Machine Profile 1.0.1 revisions, both dated 1 October 2024, were reverified from the current official specification catalogue and PDFs before encoding. Focused deterministic fake-link coverage passed all 26 codec and transport tests. The complete local validation policy passed the sealed 20-case corpus, 30 scorer tests, four summary tests, 82 host-evaluation tests with two expected sibling-worktree skips, all 246 production simulator tests with zero failures or skips, a non-empty 33-source LCOV export, the unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests and all 23 accounting-helper tests. Validation used Xcode 26.6 build 17F113 and an iPhone 17 Pro simulator running iOS 26.5. Simulator and deterministic-fake evidence is not FR30z or physical Bluetooth evidence.

The selected root Codex Desktop session is `01a084fa-1c03-74f3-958a-ccec0e04b0ba`, file `/Users/sertanyamaner/.codex/sessions/2026/09/09/rollout-2026-09-09T08-02-58-01a084fa-1c03-74f3-958a-ccec0e04b0ba.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 13 at `2026-09-09T07:05:27.984Z`: 666,813 input tokens, of which 608,640 were cached, 0 cache-write input tokens, and 3,410 output tokens, including 995 reasoning tokens, for 670,223 total tokens. The final pre-ledger snapshot is cumulative token event 78 at `2026-09-09T07:42:22.859Z`: 9,550,188 input tokens, of which 9,177,216 were cached, 0 cache-write input tokens, and 48,084 output tokens, including 16,075 reasoning tokens, for 9,598,272 total tokens. Subtracting the baseline produced 8,883,375 input tokens, of which 8,568,576 were cached, 0 cache-write input tokens, and 44,674 output tokens, including 15,080 reasoning tokens, for 8,928,049 total tokens. All 64 intervening requests reconcile exactly; one unchanged non-usage snapshot at event 62 is reported and excluded, no replay or counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 245,365 tokens. With 314,799 uncached input tokens, the token-only API-equivalent comparison estimate is `(314,799 x $4.00 + 8,568,576 x $0.40 + 44,674 x $20.00) / 1,000,000 = $5.5801064`, rounded to $5.58, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session. Separate tasks, independent exact-head review, hosted validation, this numeric insertion, subsequent checks, commit, push, pull request, merge, issue closure and tracker maintenance are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, real FTMS Control Point write, workout execution, HealthKit, watchOS, Pause/Resume surface, signing or dependency change occurred.

`PP-20260908-08` temporarily moves the slow complete Apple validation battery out of ordinary pull-request and `main`-push runs while keeping the existing full hosted Xcode and informational Codecov path available through manual workflow dispatch. Ordinary hosted runs now perform only shell syntax validation for the local entry point, production import-resource verification, the sealed corpus and deterministic scorer/summary tests, and the development-accounting helper tests on Ubuntu. The new `scripts/validate_local.sh` pins Xcode 26.6 build 17F113 and iOS Simulator SDK 26.5, uses isolated evidence paths, and runs every checked-in offline Python test, the complete production simulator suite with coverage, LCOV export, the unsigned Release simulator build, Xcode static analysis and all developer-only evaluation tests. The pull-request template requires exact local evidence, and README documentation labels the policy temporary, distinguishes local from independent hosted evidence and explains the manual reversal path. The complete new command passed on the delivery-created iPhone 17 Pro iOS 26.5 simulator: 82 offline host-evaluation tests, the sealed 20-case corpus, 30 scorer tests, four summary tests, all 220 production tests (197 unit and 23 UI) with zero failures or skips, a non-empty 30-source production LCOV export, the Release build, static analysis, all 14 developer-only evaluation tests with zero failures or skips, 23 accounting-helper tests and `git diff --check`. Actionlint also passed. No application source, test behaviour, test selection, assertion, coverage threshold, dependency lock, provider contract, credential, FTMS, treadmill, physical-device, HealthKit, watchOS or issues #48/#49 state changed; no model-provider call was made.

The selected root Codex Desktop session is `01a0828b-712e-7d11-87d7-032e03822242`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T20-42-51-01a0828b-712e-7d11-87d7-032e03822242.jsonl`, with `gpt-5.6-sol` confirmed by the session metadata. The exact baseline is cumulative token event 243 at `2026-09-08T21:24:30.433Z`: 35,378,726 input tokens, of which 34,869,504 were cached, 0 cache-write input tokens, and 45,291 output tokens, including 14,583 reasoning tokens, for 35,424,017 total tokens. The final pre-ledger snapshot is cumulative token event 277 at `2026-09-08T21:38:54.642Z`: 38,718,174 input tokens, of which 38,127,488 were cached, 0 cache-write input tokens, and 55,306 output tokens, including 18,744 reasoning tokens, for 38,773,480 total tokens. Subtracting the baseline produced 3,339,448 input tokens, of which 3,257,984 were cached, 0 cache-write input tokens, and 10,015 output tokens, including 4,161 reasoning tokens, for 3,349,463 total tokens. All 34 intervening requests reconcile exactly; no repeated cumulative snapshot, exact replay or counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 139,291 tokens. With 81,464 uncached input tokens, the token-only API-equivalent comparison estimate is `(81,464 × $4.00 + 3,257,984 × $0.40 + 10,015 × $20.00) / 1,000,000 = $1.8293496`, rounded to $1.83, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session. Issue creation before the baseline, local test-process compute, this numeric insertion, subsequent checks, commit, push, pull request, hosted CI, merge, issue closure and tracker maintenance are excluded. No provider request was made and actual provider spend was $0.00.

`PP-20260908-07` changes only `PacePromptUITests/PlansFlowUITests.swift` to remove two timing races exposed by post-merge run `34264076647`. The hosted attempt-1 xcresult showed `typeText("5.5")` completing before SwiftUI's formatted accessibility value settled, so the exact `5.5 km/h` assertion now waits on that value with a bounded predicate. Attempt-2 logs showed the shared scrolling helper repeatedly evaluating an already-hittable element, then treating existence alone as readiness and relying on a two-second destination wait; the helper now returns immediately only when the element is hittable and enabled, otherwise uses one bounded predicate for existence, hittability and enablement before tapping, and the preserved Review-destination assertion waits on the observable navigation state within the same ten-second interaction deadline. There are no sleeps, retries, skipped tests, assertion weakening, production changes or workflow changes. Both original paths passed three consecutive focused repetitions each (six tests, zero failures), then the complete production simulator suite passed all 220 tests (197 unit and 23 UI) with zero failures and zero skips on an iPhone 17 Pro simulator running iOS 26.5. Production coverage export, the unsigned Release simulator build, Xcode static analysis, import-resource verification, sealed 20-case corpus verification, 30 scorer tests, four summary-diagnostic tests, 14 developer-only evaluation tests, 23 accounting-helper tests, both repository-skill validators and `git diff --check` passed. Two earlier complete-suite attempts were not accepted: CoreSimulator runner termination affected unrelated UI tests, and the service then became unavailable; after restarting the host service and erasing only the delivery-created simulator, the final unsandboxed complete suite passed without retrying any test. No CI workflow, application source, issue #49 reducer, CoreBluetooth transport, FTMS Control Point, physical hardware, HealthKit, watchOS, dependency, provider, telemetry, signing or coverage-threshold change occurred.

The selected root Codex Desktop session is `01a0828b-712e-7d11-87d7-032e03822242`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T20-42-51-01a0828b-712e-7d11-87d7-032e03822242.jsonl`, with `gpt-5.6-sol` confirmed by the session metadata. The exact baseline is cumulative token event 9 at `2026-09-08T19:45:36.884Z`: 471,060 input tokens, of which 422,656 were cached, 0 cache-write input tokens, and 4,016 output tokens, including 1,055 reasoning tokens, for 475,076 total tokens. The final pre-ledger snapshot is cumulative token event 105 at `2026-09-08T20:24:03.105Z`: 12,944,645 input tokens, of which 12,614,784 were cached, 0 cache-write input tokens, and 25,149 output tokens, including 9,639 reasoning tokens, for 12,969,794 total tokens. Subtracting the baseline produced 12,473,585 input tokens, of which 12,192,128 were cached, 0 cache-write input tokens, and 21,133 output tokens, including 8,584 reasoning tokens, for 12,494,718 total tokens. All 95 intervening requests reconcile exactly; one unchanged non-usage snapshot at event 62 is reported and excluded, no counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 242,949 tokens. With 281,457 uncached input tokens, the token-only API-equivalent comparison estimate is `(281,457 × $4.00 + 12,192,128 × $0.40 + 21,133 × $20.00) / 1,000,000 = $6.4253392`, rounded to $6.43, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session. The issue creation before the baseline, independent exact-head review subagent, separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, GitHub Actions, Codecov, merge, issue closure and tracker maintenance are excluded. No provider request was made and actual provider spend was $0.00.

`PP-20260908-06` implements only the pure, deterministic issue #49 workout-execution boundary. It freezes one validated plan, current decoded capability snapshot, conservative session ceilings and one complete finite FR30z execution profile; models connection epochs, control permission, one correlated procedure lifecycle, telemetry evidence, observed machine state, execution phase and a monotonic `motionPossible` safety latch separately; emits typed declarative intents only; requires repeated operator readiness, current stationary telemetry and protocol acknowledgement before actuation; requires both target acknowledgements plus one later joint exact observation before a step timer can run; enforces the 30-second protocol-response and target-observation deadlines on the evidence events as well as ticks; fails closed on stale, missing, malformed, contradictory, late, duplicated, interrupted or invalidated evidence; suppresses retries, competing procedures, reconnection and resumed progression; requires explicit Stop confirmation before at most one Stop intent; and retains the separate command outcome plus human physical-stop evidence without treating ATT acceptance, protocol acknowledgement, zero-speed telemetry or silence as stop completion. The presentation projection exposes the design-required preflight blockers and activity, current and next steps, countdown, requested-versus-actual axes with command state, trustworthy elapsed/distance values, Stop status, frozen ceilings and profile identity. The final focused reducer suite passed all 44 tests. The complete production simulator suite passed all 220 tests with no failures or skips on an iPhone 17 Pro simulator running iOS 26.5; informational production coverage was 9,635 of 11,187 executable lines (86.13%), including 1,020 of 1,077 lines (94.71%) for the new reducer, and no coverage threshold was introduced. The unsigned Release simulator build, Xcode static analysis, import-resource verification, sealed 20-case corpus verification, 30 scorer tests, four summary-diagnostic tests, 14 developer-only evaluation tests, 23 accounting-helper tests, three repository-skill validators, project-file lint and staged-diff checks passed. An independent read-only preflight review found no remaining actionable issue after deadline, frozen-bound, ceiling, terminal-evidence, restart and late-procedure repairs. The initial sandboxed Release build and analysis attempts could not access CoreSimulator asset-compilation services and are not validation evidence; both gates passed when rerun with simulator-service access. No provider call, credential access, signing material, CoreBluetooth operation, FTMS Control Point encoding or write, treadmill command, physical-device session, workout execution, automatic reconnection, Pause/Resume feature, UI, History, HealthKit, watchOS, network service, analytics, account, cloud storage or new dependency occurred.

The selected root Codex Desktop session is `01a081e7-a837-7973-8dfc-01749057cff8`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T17-43-57-01a081e7-a837-7973-8dfc-01749057cff8.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 3 at `2026-09-08T16:44:50.881Z`: 103,082 input tokens, of which 84,224 were cached, 0 cache-write input tokens, and 1,391 output tokens, including 574 reasoning tokens, for 104,473 total tokens. The final pre-ledger snapshot is cumulative token event 192 at `2026-09-08T18:07:47.442Z`: 27,287,706 input tokens, of which 26,920,576 were cached, 0 cache-write input tokens, and 88,656 output tokens, including 24,901 reasoning tokens, for 27,376,362 total tokens. Subtracting the baseline produced 27,184,624 input tokens, of which 26,836,352 were cached, 0 cache-write input tokens, and 87,265 output tokens, including 24,327 reasoning tokens, for 27,271,889 total tokens. All 181 intervening requests reconcile exactly; seven exact cumulative replays and one unchanged non-usage snapshot at event 110 are reported and excluded, no counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 238,194 tokens. With 348,272 uncached input tokens, the token-only API-equivalent comparison estimate is `(348,272 × $4.00 + 26,836,352 × $0.40 + 87,265 × $20.00) / 1,000,000 = $13.8729288`, rounded to $13.87, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session. The independent preflight-review subagent, separate tasks, this numeric insertion, subsequent checks, commit, push, pull request, hosted exact-head review, GitHub Actions, Codecov, merge, issue closure and tracker maintenance are excluded. No provider request was made and actual provider spend was $0.00; no credential, physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260908-05` constrains only the complete production simulator test invocation to one test destination by passing Xcode's `-parallel-testing-enabled NO` option. Corrective PR run `34219801581` passed all 23 UI tests before a second parallel simulator clone was denied while launching the unit-test host; Xcode associated the infrastructure failure with `ImportSessionTransportTests.testLocalProtocolHTTPFailuresNeverRetry()`, while the previously corrected timeout test passed. The workflow still executes the full 153-test unit suite and full 23-test UI suite with coverage; it does not filter, skip or weaken any test. The exact one-destination CI command passed all 176 tests locally under Xcode 26.6 build 17F113 and an iPhone 17 Pro simulator running iOS 26.5, exported the same production-only LCOV result of 8,354 covered lines from 9,825, or 85.03%, and created no parallel clone. Actionlint and `git diff --check` passed. Production source, coverage scope, permissions, OIDC, action pins and Codecov reporting are unchanged. No provider call, credential access, signing material, HealthKit, watchOS, physical-device operation, FTMS Control Point write or treadmill command occurred.

The selected root Codex Desktop session is `01a07fb5-a8c2-7eb3-9c8b-4a475400b187`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T07-30-06-01a07fb5-a8c2-7eb3-9c8b-4a475400b187.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 575 at `2026-09-08T11:44:07.083Z`: 73,262,815 input tokens, of which 71,487,104 were cached, 0 cache-write input tokens, and 131,185 output tokens, including 46,846 reasoning tokens, for 73,394,000 total tokens. The final pre-ledger snapshot is cumulative token event 596 at `2026-09-08T11:53:12.827Z`: 76,948,092 input tokens, of which 75,133,824 were cached, 0 cache-write input tokens, and 134,355 output tokens, including 48,027 reasoning tokens, for 77,082,447 total tokens. Subtracting the baseline produced 3,685,277 input tokens, of which 3,646,720 were cached, 0 cache-write input tokens, and 3,170 output tokens, including 1,181 reasoning tokens, for 3,688,447 total tokens. All 21 intervening requests reconcile exactly; no repeated cumulative snapshot or counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 188,506 tokens. With 38,557 uncached input tokens, the token-only API-equivalent comparison estimate is `(38,557 × $4.00 + 3,646,720 × $0.40 + 3,170 × $20.00) / 1,000,000 = $1.6763160`, rounded to $1.68, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request update, merge, GitHub Actions execution and Codecov processing are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, treadmill command, workout execution, HealthKit, watchOS, WatchConnectivity, analytics, account, cloud storage or new application dependency occurred.

`PP-20260908-04` is the bounded test-only correction for the first post-merge execution of the CI baseline. Main run `34215815010` passed 175 of 176 production tests and all 23 UI tests, but the import timeout test observed the process-global `URLProtocol` mock count after another request had incremented it. The production transport still creates one data task and no production source changed. The corrected test attaches a unique synthetic request identifier and asks the locked mock for the count associated with that identifier, so unrelated late test traffic cannot affect the assertion while a retry of the request under test would still produce a count of two. The focused test succeeded with a 20-iteration invocation. Under Xcode 26.6 build 17F113 and an iPhone 17 Pro simulator running iOS 26.5, the complete production suite then passed all 176 tests, including all 23 UI tests; the production-only LCOV export remained 8,354 covered lines of 9,825, or 85.03%. The unsigned Release simulator build, Xcode static analysis, all 14 developer-only evaluation tests, pinned import-resource verification, the 20-case corpus check, all 30 scorer tests and all 4 summary tests also passed. The initial sandboxed Release and analysis invocations could not access CoreSimulator asset-compilation services and are not validation evidence; both gates passed when run with simulator-service access. No provider call, credential access, signing material, production behaviour, HealthKit, watchOS, physical-device operation, FTMS Control Point write or treadmill command occurred.

The selected root Codex Desktop session is `01a07fb5-a8c2-7eb3-9c8b-4a475400b187`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T07-30-06-01a07fb5-a8c2-7eb3-9c8b-4a475400b187.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 448 at `2026-09-08T10:57:42.937Z`: 58,226,950 input tokens, of which 56,619,136 were cached, 0 cache-write input tokens, and 113,247 output tokens, including 42,818 reasoning tokens, for 58,340,197 total tokens. The final pre-ledger snapshot is cumulative token event 489 at `2026-09-08T11:13:15.334Z`: 61,847,107 input tokens, of which 60,132,224 were cached, 0 cache-write input tokens, and 120,850 output tokens, including 44,673 reasoning tokens, for 61,967,957 total tokens. Subtracting the baseline produced 3,620,157 input tokens, of which 3,513,088 were cached, 0 cache-write input tokens, and 7,603 output tokens, including 1,855 reasoning tokens, for 3,627,760 total tokens. All 40 intervening requests reconcile exactly. One unchanged non-usage cumulative snapshot at event 456 is reported and excluded; no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 226,316 tokens. With 107,069 uncached input tokens, the token-only API-equivalent comparison estimate is `(107,069 × $4.00 + 3,513,088 × $0.40 + 7,603 × $20.00) / 1,000,000 = $1.9855712`, rounded to $1.99, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge, GitHub Actions execution and Codecov processing are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, treadmill command, workout execution, HealthKit, watchOS, WatchConnectivity, analytics, account, cloud storage or new application dependency occurred.

`PP-20260908-03` delivers one bounded CI and informational Codecov-baseline slice. The workflow uses the standard macOS 26 runner, verifies Xcode 26.6 build 17F113 plus the iOS 26.5 simulator environment, and runs the complete deterministic production simulator test suite with coverage, an unsigned Release simulator build, Xcode static analysis, the pinned-resource and corpus checks, scorer and summary tests, and the developer-only evaluation suite. Repository permissions default to read-only; only the isolated Codecov upload job adds `id-token: write`. Checkout, artifact transfer and Codecov actions are pinned to immutable commits, and the Codecov CLI version is fixed. The upload uses GitHub OIDC without a repository token, disables automatic coverage-file discovery and uploads only the explicitly exported LCOV artifact. Project and patch status reporting is informational, with no invented target or coverage threshold. The export traces only the instrumented production executable and rejects an empty report, missing production Swift files, or any included test target, evaluation infrastructure, design/documentation path, or DEBUG-only UI-test support file. README documentation includes the Codecov badge and states the evidence boundary.

Local validation used Xcode 26.6 build 17F113 and an iPhone 17 Pro simulator running iOS 26.5. The complete production suite passed all 176 tests with no failures, the unsigned Release simulator build and static analysis succeeded, the 14 evaluation tests passed, the pinned import resource and 20-case corpus verified, all 30 scorer tests and 4 summary tests passed, and the generated LCOV report contained only production Swift sources. That local report recorded 8,354 covered lines of 9,825, or 85.03%; this is an observed baseline, not an acceptance threshold. The official Codecov configuration validator accepted the configuration. Shell syntax, ShellCheck, actionlint, YAML parsing, `git diff --check`, staged diff review and hosted GitHub/Codecov results are recorded separately where applicable. No provider call, credential access, signing material, HealthKit, watchOS, physical-device operation, FTMS Control Point write or treadmill command occurred.

The selected root Codex Desktop session is `01a07fb5-a8c2-7eb3-9c8b-4a475400b187`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T07-30-06-01a07fb5-a8c2-7eb3-9c8b-4a475400b187.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact baseline is cumulative token event 212 at `2026-09-08T08:08:50.687Z`: 26,150,706 input tokens, of which 25,569,024 were cached, 0 cache-write input tokens, and 62,942 output tokens, including 20,834 reasoning tokens, for 26,213,648 total tokens. The final pre-ledger snapshot is cumulative token event 282 at `2026-09-08T08:47:18.875Z`: 37,032,572 input tokens, of which 36,276,224 were cached, 0 cache-write input tokens, and 87,185 output tokens, including 34,084 reasoning tokens, for 37,119,757 total tokens. Subtracting the baseline produced 10,881,866 input tokens, of which 10,707,200 were cached, 0 cache-write input tokens, and 24,243 output tokens, including 13,250 reasoning tokens, for 10,906,109 total tokens. All 70 intervening requests reconcile exactly; none crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 219,878 tokens. With 174,666 uncached input tokens, the token-only API-equivalent comparison estimate is `(174,666 × $4.00 + 10,707,200 × $0.40 + 24,243 × $20.00) / 1,000,000 = $5.4664040`, rounded to $5.47, using the GPT-5.6 Sol rates already recorded in this ledger and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge, GitHub Actions execution and Codecov processing are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, treadmill command, workout execution, HealthKit, watchOS, WatchConnectivity, analytics, account, cloud storage or new application dependency occurred.

`PP-20260908-02` delivers the bounded native SwiftUI manual-plan editor and review from designs 1f and 1g. The editor presents the canonical plan name and indoor activity, every ordered step as a scannable card, and each exact editable type, label, duration, speed and inclination value. It supports native deletion and movement plus explicit accessible move, delete and add controls, including an 18-step synthetic scroll case. Deterministic presentation mappings associate input and validation problems with the plan or exact step field while preserving unknown, unsupported, malformed and out-of-range capability states; the grouped guidance rejects invalid values without clamping or repairing them and blocks review until the draft changes. The separate review presents exact canonical identity, activity, total duration, explicitly estimated distance, step count and every ordered step, with distinct create and update confirmations. Back to edit preserves the draft, and only confirmation calls the existing local repository write path. Capability wording refers only to the currently known snapshot and contains no time, device, connection or command evidence. The layout uses semantic system colours and materials, SF Symbols, scrollable adaptive `ViewThatFits` arrangements, Dynamic Type-safe text, explicit VoiceOver labels, values, hints and stable identifiers, native touch targets, light/dark-compatible styling and no animation requiring a Reduced Motion alternative. Existing import, library, export, delete, parser, validator, schema and repository semantics remain unchanged. Focused presentation, parser, validator and view-model coverage passed all 39 tests, and the focused Plans UI suite passed all 18 tests. The complete simulator suite passed all 176 tests with no failures or skips. The Release simulator build, Xcode static analysis, all 23 accounting-helper tests, production-bundle exclusion review, `git diff --check`, complete diff review and final status review passed. The Release app contains no evaluation corpus, raw-run material, Foundation Models or XCTest linkage, UI-test scenario markers or newly authorised control path. Validation used an iPhone 17 Pro simulator running iOS 26.5. Early compile, formatting and accessibility-selector iterations were corrected before the clean acceptance runs; the initial sandboxed simulator query could not access CoreSimulator and is not validation evidence. Simulator results are software and UI evidence only, not physical-device, Bluetooth or treadmill evidence.

The selected root Codex Desktop session is `01a07fb5-a8c2-7eb3-9c8b-4a475400b187`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T07-30-06-01a07fb5-a8c2-7eb3-9c8b-4a475400b187.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact reset-aware baseline is cumulative token event 5 at `2026-09-08T06:31:03.031Z`: 190,061 input tokens, of which 162,304 were cached, 0 cache-write input tokens, and 1,278 output tokens, including 435 reasoning tokens, for 191,339 total tokens. The final pre-ledger snapshot is cumulative token event 169 at `2026-09-08T07:33:01.553Z`: 19,700,677 input tokens, of which 19,230,464 were cached, 0 cache-write input tokens, and 45,129 output tokens, including 12,236 reasoning tokens, for 19,745,806 total tokens. Subtracting the baseline produced 19,510,616 input tokens, of which 19,068,160 were cached, 0 cache-write input tokens, and 43,851 output tokens, including 11,801 reasoning tokens, for 19,554,467 total tokens. All 162 intervening requests reconcile exactly. One unchanged non-usage cumulative snapshot at event 69 is reported and excluded, event 169 is an exact replay of event 168, no counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 244,472 tokens. With 442,456 uncached input tokens, the token-only API-equivalent comparison estimate is `(442,456 × $4.00 + 19,068,160 × $0.40 + 43,851 × $20.00) / 1,000,000 = $10.2741080`, rounded to $10.27, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 8 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, treadmill command, workout execution, HealthKit, watchOS, WatchConnectivity, analytics, account, cloud storage or third-party dependency occurred.

`PP-20260908-01` delivers the bounded native SwiftUI Plans library from designs 1c, 1d and the applicable visual grammar from 1e. A deterministic presentation layer maps canonical empty, populated, protected-data unavailable, read-failure, corrupt, partial-write, unsupported-store and unsupported-plan states, plus stale and unavailable staging status, to distinct truthful content and action availability. Populated rows show only canonical name, activity, ordered step count and exact stored-duration total, with adaptive Dynamic Type layout, explicit VoiceOver label and value, and the existing editor route. The empty library describes the build, exact-review and separate-confirmation flow and exposes Create and Import while export remains disabled. Blocked states preserve data, disable mutation, remain distinct from empty, and offer retry only for protected-data and read failures. Existing import, export, edit and confirmed-delete flows remain intact; deletion now states the exact plan name and singular or plural step count. Focused Plans presentation, repository, export and validator coverage passed 59 tests, and the focused Plans UI suite passed all 13 synthetic tests. The complete simulator suite passed all 167 tests with no failures or skips. The Release simulator build, Xcode static analysis, all 23 accounting-helper tests, production-bundle exclusion review, `git diff --check`, complete diff review and final status review passed. The Release app contains no evaluation corpus, contract or raw-run artifacts, Foundation Models or XCTest linkage, UI-test scenario markers, or excluded recovery actions. Validation used an iPhone 17 Pro simulator running iOS 26.5. Early compile, assertion and accessibility-identifier iterations were corrected before the clean acceptance runs; the initial sandboxed simulator query could not access CoreSimulator and is not validation evidence. Simulator results are software and UI evidence only, not physical-device, Bluetooth or treadmill evidence.

The selected root Codex Desktop session is `01a07f83-d61f-7080-8d27-97b1e87e8b59`, file `/Users/sertanyamaner/.codex/sessions/2026/09/08/rollout-2026-09-08T06-35-41-01a07f83-d61f-7080-8d27-97b1e87e8b59.jsonl`, with `gpt-5.6-sol` confirmed by the session turn context. The exact reset-aware baseline is cumulative token event 7 at `2026-09-08T05:37:37.767Z`: 322,998 input tokens, of which 271,744 were cached, 0 cache-write input tokens, and 2,820 output tokens, including 732 reasoning tokens, for 325,818 total tokens. The final pre-ledger snapshot is cumulative token event 96 at `2026-09-08T06:12:19.661Z`: 16,358,723 input tokens, of which 16,120,832 were cached, 0 cache-write input tokens, and 29,793 output tokens, including 6,532 reasoning tokens, for 16,388,516 total tokens. Subtracting the baseline produced 16,035,725 input tokens, of which 15,849,088 were cached, 0 cache-write input tokens, and 26,973 output tokens, including 5,800 reasoning tokens, for 16,062,698 total tokens. All 89 intervening requests reconcile exactly. No counter reset or repeated cumulative snapshot occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 238,523 tokens. With 186,637 uncached input tokens, the token-only API-equivalent comparison estimate is `(186,637 × $4.00 + 15,849,088 × $0.40 + 26,973 × $20.00) / 1,000,000 = $7.6256432`, rounded to $7.63, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 8 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, treadmill command, workout execution, HealthKit, watchOS, WatchConnectivity, analytics, account, cloud storage or third-party dependency occurred.

`PP-20260907-20` delivers the bounded native SwiftUI Home entry screen from designs 1a and 1b. Bluetooth availability and treadmill evidence are presented in separate cards; Bluetooth success means only powered-on and authorised, while treadmill success requires a current connection plus successfully decoded current feature, speed-range and inclination-range evidence. Checking, resetting, unsupported, unauthorised, powered-off, idle, user-started scanning, connecting, capability discovery, incomplete, unavailable, malformed, unsupported, disconnected, stale and failed states remain explicit. Home performs no scan or connection on appearance, the existing setup route remains native, Retry reuses the existing explicit scan action, iOS Settings is offered only for unauthorised Bluetooth, and no control authority is claimed. The view uses semantic system colours, actual SF Symbols, flexible scrollable layout, Dynamic Type-safe text, combined card accessibility labels and values, native controls with standard touch targets, and no animation. Focused presentation coverage passed 8 tests and focused Home UI coverage passed 5 synthetic tests. The complete simulator suite passed all 158 tests, including 15 UI tests, with no failures or skips. The Release simulator build, Release static analysis, all 23 accounting-helper tests, the repository skill validator, production-bundle exclusion review, `git diff --check`, full diff review and final status review passed. The Release app contains no Home UI-test launch argument, synthetic scenario marker or XCTest linkage. Validation used an iPhone 17 Pro Max simulator running iOS 26.5. The initial sandboxed simulator query could not access CoreSimulator and is not validation evidence; all reported simulator gates used the authorised simulator service. Simulator results are software/UI evidence only, not physical Bluetooth or FR30z evidence.

The selected root Codex Desktop session is `01a07dec-587c-7552-8936-7229a878e664`, file `/Users/sertanyamaner/.codex/sessions/2026/09/07/rollout-2026-09-07T23-10-36-01a07dec-587c-7552-8936-7229a878e664.jsonl`, with `gpt-5.6-sol` confirmed by current task metadata. The exact reset-aware baseline is cumulative token event 8 at `2026-09-07T22:12:35.462Z`: 386,473 input tokens, of which 337,920 were cached, 0 cache-write input tokens, and 2,932 output tokens, including 1,252 reasoning tokens, for 389,405 total tokens. The final pre-ledger snapshot is cumulative token event 84 at `2026-09-07T22:52:55.033Z`: 9,954,791 input tokens, of which 9,474,688 were cached, 0 cache-write input tokens, and 44,576 output tokens, including 14,483 reasoning tokens, for 9,999,367 total tokens. Subtracting the baseline produced 9,568,318 input tokens, of which 9,136,768 were cached, 0 cache-write input tokens, and 41,644 output tokens, including 13,231 reasoning tokens, for 9,609,962 total tokens. All 75 intervening requests reconcile exactly. One unchanged cumulative snapshot at event 46 with zero metered request components is reported and excluded. No counter reset occurred, no request crossed the ledger-authorised 272,000-token long-context threshold, and the largest request input was 243,746 tokens. With 431,550 uncached input tokens, the token-only API-equivalent comparison estimate is `(431,550 × $4.00 + 9,136,768 × $0.40 + 41,644 × $20.00) / 1,000,000 = $6.2137872`, rounded to $6.21, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No provider request, credential access, external spend, physical-device or Bluetooth session, treadmill operation, FTMS Control Point write, Request Control, Start, Stop, Pause, target-setting command, automatic scan or reconnection, workout execution, HealthKit, watchOS, analytics, account, cloud storage or third-party production dependency occurred.

`PP-20260907-19` preserves the user-supplied `PacePrompt Screens.dc.html` byte-for-byte as a design reference for the current production screen family and a separately labelled, non-authorising future read-only Watch concept. The document uses the existing checked-in canvas support and design-system dependencies, maps 53 icon placements to intended SF Symbol names, keeps treadmill state neutral until current connection and capability evidence exists, and states that the production family writes nothing to the treadmill. The Watch section explicitly excludes current watchOS, HealthKit and treadmill-control authority, removes actionable Watch control, distinguishes current, stale, unavailable and disconnected treadmill evidence, and leaves the physical console and safety key authoritative. The archive's generated print copy, duplicate support/design-system files, unused icon candidates and updated legacy future-control mockup were deliberately excluded. The copied artifact and archive source have the identical SHA-256 digest `08a56b8dc2e27bea5d56697e57df9f2e851f6da2dead314d79b500fc61616b01`; its 18 local device imports are balanced, every referenced dependency already exists, no duplicate HTML identifier or external URL/network call was found, and `git diff --check` passed. No production source, Xcode target membership or runtime behaviour changed, so simulator tests, builds, device validation and treadmill operation were not applicable. The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` confirmed by current task metadata. The exact post-reset baseline is token event 947 at `2026-09-07T21:49:41.028Z`: 60,431,098 input tokens, including 59,086,464 cached and 0 cache-write input tokens, and 201,770 output tokens, including 74,081 reasoning tokens, for 60,632,868 total tokens. The final pre-ledger snapshot is event 956 at `2026-09-07T21:52:34.755Z`: 62,259,439 input tokens, including 60,891,264 cached and 0 cache-write input tokens, and 206,503 output tokens, including 77,340 reasoning tokens, for 62,465,942 total tokens. The exact delta is 1,828,341 input tokens, including 1,804,800 cached and 0 cache-write input tokens, plus 4,733 output tokens, including 3,259 reasoning tokens, for 1,833,074 total tokens. All 9 intervening requests reconcile exactly; the largest input was 215,274 tokens and none crossed the ledger-authorised 272,000-token threshold. Using the GPT-5.6 Sol rates recorded above and rechecked on 3 September 2026, the token-only API-equivalent comparison is `(23,541 × $4.00 + 1,804,800 × $0.40 + 4,733 × $20.00) / 1,000,000 = $0.910744`, rounded to $0.91. Reasoning remains a subset of output and is not added or priced again. Pre-baseline archive inspection, separate tasks, this ledger insertion, post-ledger checks, commit and push are excluded. No subagent, provider request, credential access, external spend, physical device, treadmill, FTMS write, workout execution, HealthKit or watchOS operation is included.

`PP-20260907-18` delivers the bounded software-only issue #37 execution-summary storage slice. The versioned `WorkoutExecutionSummary` preserves a complete immutable `WorkoutPlan` snapshot, optional source-plan identity, attempt and update times, one of the five accepted outcomes, explicit measured-or-unavailable duration and distance, progress, and physical-stop confirmation. Reason codes remain non-empty opaque stable values; the repository does not author or interpret execution outcomes. The separate Foundation-only `workout-history.json` repository preserves record order, appends new attempt identifiers and replaces an existing attempt in place only when its immutable fields match and its update time does not move backwards. It distinguishes empty, protected-data unavailable, read failure, corrupt, partial-write, stale-staging, unsupported store, unsupported summary and unsupported nested-plan states; closed stored JSON shapes and structural corruption block mutation. Complete protection, backup exclusion, staged validation, synchronisation and atomic replacement are required, with the previous canonical bytes preserved across every injected write-stage failure. Nothing is wired to History presentation or workout execution. Focused history coverage passed 12 tests. The complete production simulator suite passed 145 tests, including 10 UI tests, with no failures or skips. The Release simulator build, static analysis, Xcode project validation, production-bundle exclusion check, 23 accounting-helper tests, `git diff --check` and full scope review also passed. Validation used an iPhone 17 Pro simulator running iOS 26.5. The initial sandboxed focused-test attempt could not access CoreSimulator and is not validation evidence; the subsequent focused and complete runs used the authorised simulator service. Simulator tests verify requested protection operations and failure handling, not real iPhone Data Protection behaviour; physical-device validation remains unperformed and separately authorised.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 822 at `2026-09-07T20:15:19.805Z`: 43,804,690 input tokens, of which 42,864,256 were cached, 0 cache-write input tokens, and 126,063 output tokens, including 46,036 reasoning tokens, for 43,930,753 total tokens. The final pre-ledger snapshot is cumulative token event 875 at `2026-09-07T20:41:53.219Z`: 50,400,218 input tokens, of which 49,272,960 were cached, 0 cache-write input tokens, and 158,932 output tokens, including 51,069 reasoning tokens, for 50,559,150 total tokens. Subtracting the baseline produced 6,595,528 input tokens, of which 6,408,704 were cached, 0 cache-write input tokens, and 32,869 output tokens, including 5,033 reasoning tokens, for 6,628,397 total tokens. The 52 intervening requests sum exactly to those deltas. One unchanged cumulative snapshot at event 825 with zero metered request components is reported and excluded. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 220,565 tokens. With 186,824 uncached input tokens, the token-only API-equivalent comparison estimate is `(186,824 × $4.00 + 6,408,704 × $0.40 + 32,869 × $20.00) / 1,000,000 = $3.9681576`, rounded to $3.97, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. The helper reports the unrelated historical reset between token events 504 and 505 as evidence and excludes it from this later phase. This measurement includes only the root Codex task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No credential access, provider request, product network path, physical-device operation, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS operation occurred.

`PP-20260907-17` delivers the bounded software-only issue #35 export flow. A user can select one or more decoded supported saved plans, review the exact filename, category, count and every included JSON field path, then invoke a separate system Share action. The versioned export contains only the complete selected `SavedPlanRecord` values and a UTC creation time. The exporter requests complete file protection for its PacePrompt-owned temporary directory and atomic JSON file, verifies protection before sharing, cleans a previous stable artifact before replacement, and removes the prepared artifact when sharing completes or is cancelled. Repository changes after preview, blocked or stale repository states, empty selection, encoding, storage, protection and cleanup failures all fail visibly without mutating saved plans. Focused export coverage passed 10 unit and presentation tests plus one UI test. The complete production simulator suite passed 133 tests, including 10 UI tests, with no failures or skips; the developer-only evaluation target passed 14 tests. The Release simulator build, static analysis, production-bundle exclusion check, `git diff --check` and full scope review also passed. Validation used an iPhone 17 Pro simulator running iOS 26.5. Simulator evidence verifies requested protection operations and failure handling, not real iPhone Data Protection behaviour; physical-device validation remains unperformed and separately authorised.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 680 at `2026-09-07T19:02:22.093Z`: 23,503,868 input tokens, of which 22,962,688 were cached, 0 cache-write input tokens, and 71,815 output tokens, including 24,810 reasoning tokens, for 23,575,683 total tokens. The final pre-ledger snapshot is cumulative token event 791 at `2026-09-07T19:49:15.036Z`: 37,860,391 input tokens, of which 36,967,808 were cached, 0 cache-write input tokens, and 112,197 output tokens, including 39,098 reasoning tokens, for 37,972,588 total tokens. Subtracting the baseline produced 14,356,523 input tokens, of which 14,005,120 were cached, 0 cache-write input tokens, and 40,382 output tokens, including 14,288 reasoning tokens, for 14,396,905 total tokens. The 110 intervening requests sum exactly to those deltas. One unchanged cumulative snapshot with zero metered request components is reported and excluded. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 241,644 tokens. With 351,403 uncached input tokens, the token-only API-equivalent comparison estimate is `(351,403 × $4.00 + 14,005,120 × $0.40 + 40,382 × $20.00) / 1,000,000 = $7.8153000`, rounded to $7.82, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. The helper reports the unrelated historical reset between token events 504 and 505 as evidence and excludes it from this later phase. This measurement includes only the root task in the selected session; no subagent was used. Separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No credential access, provider request, product network path, physical-device operation, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS operation occurred.

`PP-20260907-16` hardens the repository-local Codex accounting skill and helper for sessions with historical counter resets. The helper now records every reset edge and decreased field, permits a snapshot and delta report only when the exact selected baseline is at or after the last reset, and exposes earlier resets as `historical_counter_resets`. Whole-session aggregation and any phase that crosses a reset still fail with an actionable error; the helper never rewrites the transcript, interpolates counters or moves the intended boundary. Regression coverage verifies default and historical snapshot capture after a reset, exact later request reconciliation, snapshot and report rejection when a reset falls inside the boundary, continued whole-session rejection, and exact baseline identity. All 23 helper tests passed, the repository skill validator passed using the pinned development-only PyYAML dependency in an isolated temporary environment, `git diff --check` passed, and a real-session smoke report reconciled the reset-bearing issue #31 session from the captured post-reset baseline.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 629 at `2026-09-07T18:32:56.801Z`: 15,385,351 input tokens, of which 14,967,040 were cached, 0 cache-write input tokens, and 47,548 output tokens, including 15,255 reasoning tokens, for 15,432,899 total tokens. The final pre-ledger snapshot is cumulative token event 645 at `2026-09-07T18:40:23.943Z`: 18,208,671 input tokens, of which 17,751,936 were cached, 0 cache-write input tokens, and 58,908 output tokens, including 20,262 reasoning tokens, for 18,267,579 total tokens. Subtracting the baseline produced 2,823,320 input tokens, of which 2,784,896 were cached, 0 cache-write input tokens, and 11,360 output tokens, including 5,007 reasoning tokens, for 2,834,680 total tokens. The 16 intervening requests sum exactly to those deltas. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 189,684 tokens. With 38,424 uncached input tokens, the token-only API-equivalent comparison estimate is `(38,424 × $4.00 + 2,784,896 × $0.40 + 11,360 × $20.00) / 1,000,000 = $1.4948544`, rounded to $1.49, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. The helper reports the unrelated historical reset between token events 504 and 505 as evidence and excludes it from this later phase. This measurement includes only the root task in the selected session; no subagent was used. Earlier issue #31 implementation and correction work, separate tasks, hosted review, the final numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No credential access, provider request, product network path, simulator or physical-device operation, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS operation occurred.

`PP-20260907-15` corrects `PP-20260907-14` after the operator explicitly requested use of the repository's established post-reset recovery procedure. The correction verified the issue #31 phase boundary against the selected session's turn and tool-call chronology, reconciled every per-request record to the cumulative delta, replaced the prior Unmeasured row and narrative, and performed arithmetic, terminology, whitespace and complete diff checks. No product source or behaviour changed, so simulator, build and test execution was not required.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact correction-phase baseline is cumulative token event 605 at `2026-09-07T18:25:47.393Z`: 11,939,597 input tokens, of which 11,554,560 were cached, 0 cache-write input tokens, and 37,611 output tokens, including 12,783 reasoning tokens, for 11,977,208 total tokens. The final pre-ledger snapshot is cumulative token event 611 at `2026-09-07T18:27:48.839Z`: 12,728,359 input tokens, of which 12,331,648 were cached, 0 cache-write input tokens, and 40,682 output tokens, including 13,878 reasoning tokens, for 12,769,041 total tokens. Subtracting the baseline produced 788,762 input tokens, of which 777,088 were cached, 0 cache-write input tokens, and 3,071 output tokens, including 1,095 reasoning tokens, for 791,833 total tokens. The six intervening per-request records sum exactly to those deltas. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 135,760 tokens. With 11,674 uncached input tokens, the token-only API-equivalent comparison estimate is `(11,674 × $4.00 + 777,088 × $0.40 + 3,071 × $20.00) / 1,000,000 = $0.4189512`, rounded to $0.42, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. The standard helper still rejects the unrelated historical reset between token events 504 and 505; this phase was calculated directly from complete monotonic post-reset events without transcript modification, interpolation or inferred counters. This measurement includes only the root task in the selected session; no subagent was used. Earlier issue #31 implementation work, separate tasks, hosted review, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub maintenance are excluded. No credential access, provider request, product network path, simulator or physical-device operation, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS operation occurred.

`PP-20260907-14` covers the bounded software-only issue #31 implementation of confirmed single-plan deletion in the Plans tab. The repository protocol now exposes the existing exact-identifier deletion operation, each saved-plan row offers a deliberate non-full-swipe destructive action, and the named permanent-deletion alert binds confirmation to the selected record identity. Cancellation performs no mutation. Confirmation invokes deletion for that exact identifier and reloads only after success; a repository failure preserves the displayed rows and presents an accurate dismissible error. Mutation remains disabled while repository loading, stale staging or another blocked state is active. Focused repository and presentation tests passed 38 tests, the focused deletion UI test passed, and the complete production simulator suite passed 122 tests, including 9 UI tests, with no failures or skips. Simulator build and static analysis also passed, together with `git diff --check` and full scope review. Validation used an iPhone 17 Pro simulator running iOS 26.5. The first two exploratory UI runs exposed and repaired confirmation accessibility issues and are not acceptance evidence.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 520 at `2026-09-07T05:22:14.693Z`, the last complete event before the failed baseline-snapshot command: 1,288,712 input tokens, of which 1,212,032 were cached, 0 cache-write input tokens, and 6,086 output tokens, including 2,628 reasoning tokens, for 1,294,798 total tokens. The final pre-ledger snapshot is cumulative token event 572 at `2026-09-07T05:40:15.012Z`: 9,658,540 input tokens, of which 9,414,272 were cached, 0 cache-write input tokens, and 25,827 output tokens, including 9,095 reasoning tokens, for 9,684,367 total tokens. Subtracting the baseline produced 8,369,828 input tokens, of which 8,202,240 were cached, 0 cache-write input tokens, and 19,741 output tokens, including 6,467 reasoning tokens, for 8,389,569 total tokens. The 51 intervening per-request records sum exactly to those deltas. One unchanged cumulative snapshot with zero metered request components is excluded. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 233,332 tokens. With 167,588 uncached input tokens, the token-only API-equivalent comparison estimate is `(167,588 × $4.00 + 8,202,240 × $0.40 + 19,741 × $20.00) / 1,000,000 = $4.346068`, rounded to $4.35, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 7 September 2026. Reasoning remains a subset of output and is not added or priced again. The standard helper rejects the unrelated historical cumulative decrease between session token events 504 and 505; following the operator's explicit correction instruction and the established `PP-20260906-09` procedure, this bounded phase was calculated directly from the complete monotonic post-reset events without rewriting the transcript, interpolation or inferred counters. This measurement scope includes only the root task in that selected session; no subagent was used. Separate tasks, hosted review, external provider execution, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. No credential access, provider request, network product path, simulator Bluetooth write, physical device, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS operation occurred.

`PP-20260906-13` covers the autonomous software-only issue #29 correction after current `main` was found to describe the accepted production workout-import path as absent. The README now states the delivered per-request disclosure and consent boundary, selected OpenRouter/OpenAI Sol route, device-only Keychain storage, strict response parsing, deterministic local mapping and capability validation, exact preview and separate save confirmation. It also retains the exclusions for automatic/background inference, provider fallback, Apple Foundation Models production integration, accounts, analytics, cloud storage, prompt/response history, deletion/recovery UI, workout history, treadmill commands, FTMS Control Point writes, workout execution, HealthKit and watchOS. Current production source and the accepted production contract were compared directly; the three existing local documentation links, stale-claim search, current-claim search, `git diff --check` and full diff review passed. No product source, project, test, resource, request, model, route or behaviour changed, so simulator/build/test execution was not required for this documentation-only slice.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 471 at `2026-09-06T22:09:19.531Z`: 63,709,424 input tokens, of which 62,573,184 were cached, 0 cache-write input tokens, and 128,637 output tokens, including 48,516 reasoning tokens, for 63,838,061 total tokens. The final pre-ledger snapshot is cumulative token event 478 at `2026-09-06T22:11:15.194Z`: 65,204,992 input tokens, of which 64,056,320 were cached, 0 cache-write input tokens, and 131,796 output tokens, including 49,533 reasoning tokens, for 65,336,788 total tokens. Subtracting the baseline produced 1,495,568 input tokens, of which 1,483,136 were cached, 0 cache-write input tokens, and 3,159 output tokens, including 1,017 reasoning tokens, for 1,498,727 total tokens. The seven intervening requests sum exactly to those deltas. None crossed the ledger-authorised 272,000-token threshold; the largest input was 217,775 tokens. With 12,432 uncached input tokens, the token-only API-equivalent comparison estimate is `(12,432 × $4.00 + 1,483,136 × $0.40 + 3,159 × $20.00) / 1,000,000 = $0.7061624`, rounded to $0.71, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 6 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex Desktop task in the selected session; no subagent was used. Separate tasks, hosted review, external provider execution, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. No credential access, provider request or OpenRouter spend occurred. No simulator, physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-12` covers the authorised second issue #19 compatibility repair after the immutable post-`PP-20260906-11` DEBUG build made one request with the unchanged frozen request and again passed endpoint, HTTP status, content type, JSON syntax, provider/model identity, outer allowlist and contract, and service-tier validation before failing closed at the usage contract. Current public OpenRouter response evidence showed a non-semantic `image_tokens` completion counter on text-only responses and the OpenAI route's provider-native terminal value `completed` alongside normalized `finish_reason: "stop"`. The production parser now admits only a nullable nonnegative integer `image_tokens` and exact native `completed`; negative, fractional, nonnumeric and unknown accounting fields, and adjacent native-finish values, still fail closed. No image or tool capability, request bytes, routing, schema, mapping, local validation, preview or save authority changed. The focused import run passed 32 tests, the complete production simulator suite passed 117 tests, and the evaluation suite passed 14 tests, all without failures or skips. The deterministic scorer passed 30 tests and verified the unchanged 20-case corpus. Release simulator build, Release static analysis, pinned-resource/source-privacy verification, built-bundle independence, Release executable diagnostic-string exclusion, 20 accounting-helper tests, all three repository skill validators, `git diff --check` and full diff review passed.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 290 at `2026-09-06T21:37:19.287Z`: 37,752,608 input tokens, of which 37,101,312 were cached, 0 cache-write input tokens, and 89,515 output tokens, including 35,536 reasoning tokens, for 37,842,123 total tokens. The final pre-ledger snapshot is cumulative token event 333 at `2026-09-06T21:48:42.888Z`: 43,052,219 input tokens, of which 42,314,112 were cached, 0 cache-write input tokens, and 99,680 output tokens, including 37,944 reasoning tokens, for 43,151,899 total tokens. Subtracting the baseline produced 5,299,611 input tokens, of which 5,212,800 were cached, 0 cache-write input tokens, and 10,165 output tokens, including 2,408 reasoning tokens, for 5,309,776 total tokens. The 41 intervening requests sum exactly to those deltas. One exact replay and one unchanged cumulative snapshot with zero metered request components are reported but not counted. None crossed the ledger-authorised 272,000-token threshold; the largest input was 224,033 tokens. With 86,811 uncached input tokens, the token-only API-equivalent comparison estimate is `(86,811 × $4.00 + 5,212,800 × $0.40 + 10,165 × $20.00) / 1,000,000 = $2.6356640`, rounded to $2.64, using the GPT-5.6 Sol rates already recorded in this ledger with pricing date 6 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex Desktop task in the selected session; no subagent was used. Separate tasks, hosted review, external provider execution, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. Both DEBUG provider requests preceded this measured phase; their spend remains Unmeasured. No credential bytes were accessed or retained, and no physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-11` covers the authorised issue #19 compatibility repair after one sealed DEBUG request from remote main `03686208b13014224ee2dfbd391db87de7382947` passed endpoint, HTTP status, content type, JSON syntax, provider/model identity, outer allowlist and contract, and service-tier validation before failing closed at the usage contract. The public OpenRouter OpenAPI documented the missing optional accounting-only fields: nullable `server_tool_use_details` with a closed set of three nullable nonnegative integer counters, and nullable nonnegative `server_tool_cost` within `cost_details`. The production parser now admits only those documented shapes; unknown fields and invalid values still fail closed, and no tool support, request, routing, schema, mapping, capability, preview or save authority changes. The focused import run passed 32 tests, the complete production simulator suite passed 117 tests and the evaluation suite passed 14 tests, all without failures or skips. The Release simulator build, Release static analysis, pinned-resource/source-privacy verification, built-bundle independence, Release executable diagnostic-string check, 20 accounting-helper tests, `git diff --check` and full diff review passed. Two initially parallel follow-on Xcode jobs lost CoreSimulator service and are not acceptance evidence; both affected gates subsequently passed serially.

The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 178 at `2026-09-06T21:15:02.756Z`: 23,938,779 input tokens, of which 23,530,496 were cached, 0 cache-write input tokens, and 64,189 output tokens, including 25,604 reasoning tokens, for 24,002,968 total tokens. The final pre-ledger snapshot is cumulative token event 213 at `2026-09-06T21:25:52.079Z`: 28,036,075 input tokens, of which 27,515,264 were cached, 0 cache-write input tokens, and 71,379 output tokens, including 27,322 reasoning tokens, for 28,107,454 total tokens. Subtracting the baseline produced 4,097,296 input tokens, of which 3,984,768 were cached, 0 cache-write input tokens, and 7,190 output tokens, including 1,718 reasoning tokens, for 4,104,486 total tokens. The 34 intervening requests sum exactly to those deltas. None crossed the ledger-authorised 272,000-token threshold; the largest input was 221,438 tokens. One unchanged cumulative snapshot after context maintenance reported zero metered request components and is not counted. With 112,528 uncached input tokens, the token-only API-equivalent comparison estimate is `(112,528 × $4.00 + 3,984,768 × $0.40 + 7,190 × $20.00) / 1,000,000 = $2.1878192`, rounded to $2.19, using the GPT-5.6 Sol rates recorded above with pricing date 6 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex Desktop task in the selected session; no subagent was used. Separate tasks, hosted review, provider execution, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. The single provider request preceded this measured phase; its spend remains Unmeasured. No credential bytes were accessed or retained, and no physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-10` covers the authorised issue #26 milestone: transfer of the separate `PP-20260906-09` accounting change into an isolated worktree, DEBUG-only closed-enum diagnostics for disclosure, request lifecycle, transport, response-envelope and structured-content boundaries, mapping, local validation, preview eligibility and terminal outcome, an injectable observational sink, redaction and one-request assertions, documentation, Release exclusion, complete validation and full diff review. The production parser remains the sole acceptance authority; the diagnostic inspector cannot alter routing, retries, parsing, mapping, preview or save. The focused import suite passed 25 tests, the complete production simulator suite passed 117 tests, the evaluation suite passed 14 tests, the accounting helper passed 20 tests, and Release build, Release static analysis, pinned-resource/source-privacy verification, built-bundle independence, Release executable string checks and `git diff --check` passed. The selected root Codex Desktop session is `01a0786f-279a-7b60-9e72-58cb13cbfda6`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T21-35-45-01a0786f-279a-7b60-9e72-58cb13cbfda6.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 18 at `2026-09-06T20:39:18.822Z`: 1,136,067 input tokens, of which 1,064,960 were cached, 0 cache-write input tokens, and 8,348 output tokens, including 1,279 reasoning tokens, for 1,144,415 total tokens. The final pre-ledger snapshot is cumulative token event 81 at `2026-09-06T20:56:39.865Z`: 10,700,535 input tokens, of which 10,432,384 were cached, 0 cache-write input tokens, and 35,725 output tokens, including 12,136 reasoning tokens, for 10,736,260 total tokens. Subtracting the baseline produced 9,564,468 input tokens, of which 9,367,424 were cached, 0 cache-write input tokens, and 27,377 output tokens, including 10,857 reasoning tokens, for 9,591,845 total tokens. The 62 intervening requests sum exactly to those deltas; none crossed the 272,000-token threshold and the largest input was 236,407 tokens. With 197,044 uncached input tokens, the token-only API-equivalent comparison estimate is `(197,044 × $4.00 + 9,367,424 × $0.40 + 27,377 × $20.00) / 1,000,000 = $5.0826856`, rounded to $5.08, using the GPT-5.6 Sol rates recorded above with pricing date 6 September 2026. Reasoning remains a subset of output and is not added or priced again. This measurement includes only the root Codex Desktop task in the selected session; no subagent was used. Separate tasks, hosted review, external provider execution, this numeric insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. No real credential was accessed, no provider request was made and OpenRouter spend was $0.00. No physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-09` records the operator-requested recovery of exact Codex counts for the issue #19/#26 handover without combining it with the earlier Astra/Sol phases. The selected root Codex Desktop session is `01a07737-423f-7070-a427-4d190ee8651a`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T15-55-05-01a07737-423f-7070-a427-4d190ee8651a.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event 939 at `2026-09-06T20:26:04.870Z`: 120,946,733 input tokens, of which 119,106,688 were cached, 0 cache-write input tokens, and 243,421 output tokens, including 94,192 reasoning tokens, for 121,190,154 total tokens. The final pre-ledger snapshot is cumulative token event 943 at `2026-09-06T20:27:22.348Z`: 121,394,752 input tokens, of which 119,534,592 were cached, 0 cache-write input tokens, and 244,949 output tokens, including 94,967 reasoning tokens, for 121,639,701 total tokens. Subtracting the baseline produced 448,019 input tokens, of which 427,904 were cached, 0 cache-write input tokens, and 1,528 output tokens, including 775 reasoning tokens, for 449,547 total tokens. The four intervening per-request records sum exactly to those deltas. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 117,298 tokens. With 20,115 uncached input tokens, the token-only API-equivalent comparison estimate is `(20,115 × $4.00 + 427,904 × $0.40 + 1,528 × $20.00) / 1,000,000 = $0.2821816`, rounded to $0.28, using the GPT-5.6 Sol rates recorded above and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. The standard helper still rejects the unrelated historical cumulative decrease between session token events 77 and 78; this bounded phase is calculated directly from the complete monotonic post-reset events without rewriting the transcript, interpolation or inferred counters. This measurement includes only the root Codex Desktop task in the selected session. No subagent was used. Earlier Astra/Sol work, external provider execution, this numeric insertion, the handover prompt, subsequent checks, any commit, push, pull request, merge or GitHub issue maintenance are excluded. This measured phase made no code change, credential access, provider request or OpenRouter spend and used no physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS.

`PP-20260906-08` covers the operator-authorised contract amendment following the third sealed simulator observation on remote main `7ce397103367fd32ece857d9d86c17219668a408`, which made exactly one provider request and failed closed as `identityModelAlias` while the separately inspected provider-side generation record attributed the generation to the selected canonical revision and OpenAI route. The parser now validates model and provider as one tuple, accepting either the canonical revision or the exact requested alias only with provider `openai` or `OpenAI`; missing, non-string, unqualified or unrelated models and missing or different providers still fail closed. Request construction, routing, privacy controls, retries, response schema, mapping, local validation and save authority are unchanged. Synthetic regression coverage includes both accepted provider spellings and rejected provider variants. The focused boundary suite passed 22 tests; the complete production simulator suite passed 114 tests; the evaluation simulator suite passed 14 tests; Release simulator build, static analysis, resource provenance and bundle-independence verification, whitespace checks and complete diff review passed. The selected root Codex Desktop session is `01a07737-423f-7070-a427-4d190ee8651a`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T15-55-05-01a07737-423f-7070-a427-4d190ee8651a.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. Both the pre-implementation and final helper attempts rejected the inherited historical cumulative decrease between token-count events 77 and 78. In accordance with repository policy, input, cached input, cache-write input, output, reasoning subset, total, request count, largest request, threshold crossings and API-equivalent cost remain Unmeasured; no transcript repair, interpolation or inferred delta was used. This row insertion, subsequent checks, commit, push, pull request, merge and GitHub issue maintenance are excluded. No subagent was used. This implementation made no credential access, provider request or OpenRouter spend and did not use a physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS.

`PP-20260906-07` covers the authorised second-stage diagnostic after the simulator observation on remote main `175b8279d5047b6e9ec1f19f94076f667560191d` failed closed as `identityModelMismatch`: a clean isolated worktree, redacted classification of the top-level response model as the requested alias, canonical revision without provider prefix, non-string value or other mismatch, synthetic privacy and feedback assertions, production-contract and handoff updates, current public OpenRouter model-response and canonical-slug review, 113 complete production simulator tests, 14 evaluation simulator tests, Release simulator build, static analysis, resource/bundle verification and complete diff review. Every noncanonical category remains rejected; no received value or response body is displayed or retained, and the accepted request, model, revision, provider, envelope, retry and local-validation contracts are unchanged. The selected root Codex Desktop session is `01a07737-423f-7070-a427-4d190ee8651a`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T15-55-05-01a07737-423f-7070-a427-4d190ee8651a.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The unmodified accounting helper rejected the known historical cumulative decrease between token-count events 77 and 78 before producing a baseline. In accordance with the repository accounting policy, input, cached input, cache-write input, output, reasoning subset, total, request count, largest request, threshold crossings and API-equivalent cost remain Unmeasured; no transcript repair, interpolation or inferred delta was used. This row insertion, subsequent checks, commit, push, pull request and merge are excluded. No subagent was used. No credential was read or changed, no provider request was made and actual OpenRouter spend was $0.00. No physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-06` covers the authorised post-live diagnostic slice for issue #19: exact remote-main authority refresh at `272dc30db219e70d5db9cfae58ca2fcdd7ed8be0`, isolated-worktree implementation, stable redacted outcome codes for response URL, canonical model, provider, service tier, message-model, redirect and response content type failures, focused privacy assertions, contract documentation, complete simulator validation and full diff review. The selected root Codex Desktop session is `01a07737-423f-7070-a427-4d190ee8651a`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T15-55-05-01a07737-423f-7070-a427-4d190ee8651a.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact baseline is cumulative token event ordinal 3089 at `2026-09-06T18:07:40.740Z`: 49,798,271 input tokens, of which 49,114,880 were cached, 0 cache-write input tokens, and 96,077 output tokens, including 39,640 reasoning tokens, for 49,894,348 total tokens. The final pre-ledger snapshot is cumulative token event ordinal 3695 at `2026-09-06T18:28:04.628Z`: 62,956,376 input tokens, of which 62,070,400 were cached, 0 cache-write input tokens, and 117,888 output tokens, including 48,334 reasoning tokens, for 63,074,264 total tokens. Subtracting the baseline produced 13,158,105 input tokens, of which 12,955,520 were cached, 0 cache-write input tokens, and 21,811 output tokens, including 8,694 reasoning tokens, for 13,179,916 total tokens. The 84 intervening per-request records sum exactly to those deltas. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 225,365 tokens. With 202,585 uncached input tokens, the token-only API-equivalent comparison estimate is `(202,585 × $4.00 + 12,955,520 × $0.40 + 21,811 × $20.00) / 1,000,000 = $6.428768`, rounded to $6.43, using the GPT-5.6 Sol rates recorded above and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. The standard helper rejected an unrelated earlier cumulative decrease between session token events 77 and 78, both before this phase; following the operator's explicit instruction, this row was calculated directly from the complete monotonic phase events without rewriting the transcript, interpolating values or repairing any event. This measurement includes only the root Codex Desktop task in the selected session. No subagent was used. The final ledger insertion, subsequent checks, commit, push, pull-request and merge operations are excluded. No credential was read or changed, no provider request was made and actual OpenRouter spend was $0.00. No physical device, treadmill, FTMS Control Point, workout execution, HealthKit or watchOS operation is included.

`PP-20260906-05` covers the authorised repair of the fresh-install saved-plan storage failure: exact-head isolated-worktree setup, Foundation error diagnosis, Application Support creation, correct handling of Cocoa missing-file codes 4 and 260, two focused regression tests, 19 focused repository tests, 109 complete simulator tests, signed Release builds, static analysis, production-resource verification, fresh-install Release UI observation, complete diff review, manual accounting reconciliation, remote-authority refresh and branch preparation. The selected root Codex Desktop session is `01a07737-423f-7070-a427-4d190ee8651a`, file `/Users/sertanyamaner/.codex/sessions/2026/09/06/rollout-2026-09-06T15-55-05-01a07737-423f-7070-a427-4d190ee8651a.jsonl`, with `gpt-5.6-sol` recorded in the phase turn context. The exact start snapshot is token event ordinal 1177 at `2026-09-06T16:56:36.419Z`: 13,404,264 input tokens, of which 13,148,544 were cached, 0 cache-write input tokens, and 17,825 output tokens, including 6,493 reasoning tokens, for 13,422,089 total tokens. The final pre-ledger snapshot is token event ordinal 1763 at `2026-09-06T17:17:01.514Z`: 24,310,590 input tokens, of which 23,901,184 were cached, 0 cache-write input tokens, and 41,132 output tokens, including 15,417 reasoning tokens, for 24,351,722 total tokens. Subtracting the start produced 10,906,326 input tokens, of which 10,752,640 were cached, 0 cache-write input tokens, and 23,307 output tokens, including 8,924 reasoning tokens, for 10,929,633 total tokens. The 78 intervening per-request records sum exactly to those deltas. None crossed the ledger-authorised 272,000-token long-context threshold; the largest request input was 210,774 tokens. With 153,686 uncached input tokens, the token-only API-equivalent comparison estimate is `(153,686 × $4.00 + 10,752,640 × $0.40 + 23,307 × $20.00) / 1,000,000 = $5.3819400`, rounded to $5.38, using the GPT-5.6 Sol rates recorded above and rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. The standard helper rejected an unrelated earlier cumulative decrease between session token events 77 and 78, both before this phase; following the operator's explicit instruction, this row was calculated directly from the complete monotonic phase events without rewriting the transcript, interpolating values or repairing any event. This measurement includes only the root Codex Desktop task in the selected session. No subagent was used. Separate tasks, external provider execution, this final numeric ledger insertion, post-insertion checks, commit, push and GitHub operations are excluded. No credential was accessed, no provider call was made and actual OpenRouter spend was $0.00.

`PP-20260906-04` covers the authorised issue #19 production import slice: explicit disclosure and per-generation consent; device-only, non-synchronising Keychain credential handling; one independent ephemeral request transport with no cookies, cache or retry; frozen production request resources; strict response allowlisting and canonical identity checks; deterministic mapping into the existing untrusted proposal/validator/preview/save path; lifecycle and capability invalidation; DEBUG-only synthetic UI-test seams; offline resource provenance and bundle-independence verification; and the complete local review and validation battery. The review found one test-only gap: the suite did not compare the complete outbound request body to the frozen fixture. The correction captures the production request, asserts its method, URL, 60-second timeout, reload-ignoring-cache policy and exact three-header envelope, then compares the whole decoded body with `docs/import/outbound-request.json`. Production code and frozen resource bytes were unchanged. Mocked and simulator success is not live-provider acceptance. No credential was read, no provider call or spend occurred, and no physical device, treadmill, FTMS Control Point, HealthKit or watchOS operation is included. No subagent was used. Push, publication, issue closure and any future live-provider or hardware gate are excluded.

The accounting retains four exact, non-overlapping phases rather than merging usage across models or task boundaries. Reasoning is a subset of output in every phase and is not added or priced twice. All requests stayed below the published 272,000-input-token long-context threshold, so no long-context multiplier applies. Standard OpenAI text-token prices were checked on 6 September 2026: GPT-6 Astra at $10.00 per million uncached input, $1.00 cached input and $50.00 output; GPT-5.6 Sol at $4.00 per million uncached input, $0.40 cached input and $20.00 output. These are token-only API-equivalent comparisons for ChatGPT subscription sessions, not API bills.

- Astra implementation phase, session `01a076c2-21c1-78a0-9d40-caa524fc0178`, events 4 through 71: 10,920,072 input tokens, including 10,751,872 cached and 0 cache-write tokens; 56,126 output tokens, including 15,006 reasoning tokens; 10,976,198 total tokens; 67 requests; largest request 209,321 tokens. With 168,200 uncached input tokens, the exact API-equivalent is `(168,200 × $10.00 + 10,751,872 × $1.00 + 56,126 × $50.00) / 1,000,000 = $15.240172`, rounded to $15.24.
- Sol review phase, session `01a07702-324c-7b40-b10a-168b64a3a6c3`, events 4 through 32: 2,485,216 input tokens, including 2,399,104 cached and 0 cache-write tokens; 11,058 output tokens, including 6,590 reasoning tokens; 2,496,274 total tokens; 28 requests; largest request 126,045 tokens. With 86,112 uncached input tokens, the exact API-equivalent is `(86,112 × $4.00 + 2,399,104 × $0.40 + 11,058 × $20.00) / 1,000,000 = $1.5252496`, rounded to $1.53.
- Sol test-fix and revalidation phase, the same session, from the event-37 baseline through its event-73 final snapshot: 6,335,687 input tokens, including 6,263,168 cached and 0 cache-write tokens; 7,445 output tokens, including 2,003 reasoning tokens; 6,343,132 total tokens; 36 requests; largest request 201,998 tokens. With 72,519 uncached input tokens, the exact API-equivalent is `(72,519 × $4.00 + 6,263,168 × $0.40 + 7,445 × $20.00) / 1,000,000 = $2.9442432`, rounded to $2.94.
- Sol authorised pre-commit preparation phase, the same session, events 85 through 89: 168,956 input tokens, including 156,672 cached and 0 cache-write tokens; 1,143 output tokens, including 188 reasoning tokens; 170,099 total tokens; 4 requests; largest request 48,029 tokens. With 12,284 uncached input tokens, the exact API-equivalent is `(12,284 × $4.00 + 156,672 × $0.40 + 1,143 × $20.00) / 1,000,000 = $0.1346648`, rounded to $0.13.

The original implementation evidence remains `/private/tmp/paceprompt-issue19-usage.json`; its same-token Astra/Sol comparison remains `/private/tmp/paceprompt-issue19-cost-comparison.json`. The continuation baselines remain `/private/tmp/paceprompt-issue19-review-baseline.json`, `/private/tmp/paceprompt-issue19-testfix-baseline.json` and `/private/tmp/paceprompt-issue19-commit-baseline.json`. The final ledger insertion, post-insertion checks and local commit are outside the measured phase. The commit carries `PacePrompt-Change: PP-20260906-04`; push is not authorised.

`PP-20260906-03` covers the authorised report extension: scheduled and recorded-label-only classification metrics, exact-fraction JSON, per-class tables, twelve count/row-percentage confusion matrices, front-loaded results, dataset construction and deterministic-scoring methodology, and the reproducible standard-library generator with four focused tests. Source inspection, per-model/per-category support reconciliation, regeneration checks, main-table/link checks, representative chart inspection and diff review were performed. Frozen evaluation data, scorer, policies and eligibility are unchanged; no app code, live inference, credentials or hardware were used. The selected root Desktop session is `01a072ba-aaa9-7543-ba04-cd4dc59945a1`; the original-record helper again rejected the earlier cumulative decrease between events 51 and 52 at baseline and final capture. Input, cached input, cache-write input, output, reasoning subset, total, request count, largest request and threshold crossings remain Unmeasured; no API-equivalent cost is inferred. No agents were spawned. Separate tasks and provider usage are excluded, as are final ledger insertion, subsequent checks and Git operations. No transcript rewriting or accounting-helper change was attempted in this phase.

`PP-20260906-02` covers the user-authorised documentation-only consolidation of v2.9 screening, v3 confirmatory and v4 open-weight results, source-hash and exact aggregate comparisons, category and eligibility tables, four visually inspected charts, link checks and diff review. The selected root Desktop session is `01a072ba-aaa9-7543-ba04-cd4dc59945a1`; its turn context records `gpt-6-astra`. Baseline and final attempts with the unchanged helper rejected a cumulative decrease between token-count events 51 and 52, before this phase. Investigation confirmed an earlier counter reset. A proposed reduced transcript was blocked before creation because it could bypass whole-record integrity validation; neither the transcript nor the helper was modified. Exact phase input, cached input, cache-write input, output, reasoning subset, total, request count, largest request and threshold crossings remain Unmeasured. No API-equivalent cost is inferred. No subagents or live provider calls were used. Separate tasks and provider execution are excluded; final ledger insertion, post-insertion checks and Git operations are outside the attempted phase boundary. This limitation does not affect the independently hash-verified model-evaluation metrics.

`PP-20260906-01` covers the authorised GitHub issue #17 open-weight research and candidate matrix; the staged curl compatibility probes and replacement-route selection; the nine-route v4 host evaluator with native JSON Schema and forced-tool response contracts; mocked payload, route, privacy, pacing, timeout, retry, cancellation, rate-limit and spending controls; the live evaluation and two preserved harness-failure investigations; the scorer-failure continuation repair; the mechanically accepted aggregate; the explicitly ratified decision that no open-weight candidate qualifies and `openai/gpt-5.6-sol` remains selected; the reviewed non-sensitive summary; 82 host tests; legacy v3 and open-weight v4 verification; Python compilation; complete simulator tests and build; static analysis; credential-leak checks; and the full staged diff review. An exact baseline was requested from root Codex Desktop session `01a072ba-aaa9-7543-ba04-cd4dc59945a1`, but `phase_usage.py` rejected token-count event 256 because it accompanied an unchanged cumulative snapshot while retaining non-zero input, cached-input, output and reasoning-output request counters. In accordance with the accounting policy, no baseline, final delta, request count, largest request, threshold result, model identifier or API-equivalent cost was reconstructed. No subagent was created; separate tasks, hosted review, OpenRouter provider execution and GitHub execution are outside Codex token accounting. Provider-reported evaluation charges are operational evidence recorded in the issue #17 aggregate summary, not development API-equivalent cost. No production inference, API-key UI or persistence, physical-iPhone operation, Apple Foundation Models inference, treadmill operation, FTMS Control Point write, workout execution, HealthKit or watchOS work is included. This row insertion, post-insertion checks, commit, push and GitHub issue maintenance are excluded.

`PP-20260902-01` covers the full Codex Desktop task from repository orientation through implementation, simulator validation, the user-run physical capability check, evidence reconciliation and pre-commit review. The cutoff is the final counter snapshot before inserting this numeric row; the small final insertion, commit and push operations are excluded. No linked subagent usage is included.

`PP-20260902-02` covers the implementation and simulator validation of GitHub issue #1, committed as `5aea8ca` with the matching `PacePrompt-Change` trailer. The task interface did not expose an exact starting counter snapshot, ending counter snapshot or model identifier, so no token count or API-equivalent cost was reconstructed.

`PP-20260902-03` covers the GitHub issue #2 protocol specification, integration with `e41bbae`, primary-source review, documentation checks and simulator validation. The exact cumulative task snapshot immediately before commit `b2d6526` reported 12,396,013 input tokens, of which 11,889,664 were cached, 0 cache-write input tokens, and 50,316 output tokens, including 24,968 reasoning tokens. The total was 12,446,329 tokens. The model was `gpt-5.6-sol`. The largest request input was 215,462 tokens, so none of the 101 measured requests crossed the published 272,000-token long-context threshold. With 506,349 uncached input tokens, the token-only API-equivalent is `(506,349 × $4.00 + 11,889,664 × $0.40 + 50,316 × $20.00) / 1,000,000 = $7.7875816`, rounded to $7.79. This is a task aggregate because no separate zero-valued start snapshot was recorded; the final numeric insertion, commit and push are excluded.

`PP-20260903-01` covers the follow-up investigation of passive FTMS telemetry, the Training Status initial-read implementation, simulator validation and the user-operated physical-treadmill capture. The capture confirmed successful subscriptions to `0x2ACD`, `0x2AD3` and `0x2ADA`, an initial `0x2AD3` value of `00 00`, and live `0x2ACD` notifications while the belt was operated exclusively from the physical console. No Control Point write was performed. This task interface did not expose exact start/end counters or a reliable exact model identifier, so token usage and API-equivalent cost remain unmeasured.

`PP-20260903-02` covers recovery of the exact local session counters, request-level threshold checking, pricing verification and this accounting correction. Subtracting the exact phase-start snapshot from the final snapshot produced 2,075,901 input tokens, of which 1,902,464 were cached, 0 cache-write input tokens, and 7,522 output tokens, including 4,983 reasoning tokens. The total phase delta was 2,083,423 tokens. No request crossed the 272,000-token threshold. With 173,437 uncached input tokens, the token-only API-equivalent is `(173,437 × $4.00 + 1,902,464 × $0.40 + 7,522 × $20.00) / 1,000,000 = $1.6051736`, rounded to $1.61. The final numeric insertion, commit and push are excluded.

`PP-20260903-03` covers the authorised GitHub issue #3 source review, user-ratified pure workout-plan schema and validator implementation, synthetic focused and complete simulator tests, simulator build, static analysis, autonomous completion audit and final diff review. The exact phase-start snapshot reported 691,725 input tokens, of which 610,304 were cached, 0 cache-write input tokens, and 8,358 output tokens, including 6,039 reasoning tokens. The final snapshot reported 11,389,473 input tokens, of which 11,177,856 were cached, 0 cache-write input tokens, and 44,303 output tokens, including 19,659 reasoning tokens. Subtracting the start snapshot produced 10,697,748 input tokens, of which 10,567,552 were cached, 0 cache-write input tokens, and 35,945 output tokens, including 13,620 reasoning tokens. The total phase delta was 10,733,693 tokens. None of the 67 measured implementation-phase requests crossed the 272,000-token threshold; the largest request input was 205,658 tokens. With 130,196 uncached input tokens, the token-only API-equivalent is `(130,196 × $4.00 + 10,567,552 × $0.40 + 35,945 × $20.00) / 1,000,000 = $5.4667048`, rounded to $5.47. No subagent usage, physical-device operation or FTMS Control Point write is included. The final numeric insertion, commit and push are excluded.

`PP-20260903-04` covers the authorised GitHub issue #8 repository-local phase-accounting skill and standard-library helper, synthetic acceptance tests, bootstrap verification against the selected live session, the pinned development-only PyYAML dependency and isolated validator installation, skill validation, complete simulator tests, simulator build, static analysis and full diff review. PyYAML is used only by Codex's bundled skill validator; the helper and its tests retain a standard-library-only runtime. Because the helper did not exist at phase start, the bootstrap boundary was captured directly from token-count event 3 in session `01a06689-8622-7670-9847-7790f6ec0153`, then re-materialised and verified after the helper existed. The exact phase-start snapshot reported 122,369 input tokens, of which 91,008 were cached, 0 cache-write input tokens, and 1,949 output tokens, including 943 reasoning tokens. The final pre-ledger snapshot reported 9,642,111 input tokens, of which 9,368,960 were cached, 0 cache-write input tokens, and 75,655 output tokens, including 28,549 reasoning tokens. Subtracting the start snapshot produced 9,519,742 input tokens, of which 9,277,952 were cached, 0 cache-write input tokens, and 73,706 output tokens, including 27,606 reasoning tokens. The total phase delta was 9,593,448 tokens. None of the 83 measured implementation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 214,632 tokens. One unchanged cumulative snapshot after context maintenance reported zero metered request components plus a standalone 21,593-token context-size value; the helper reports that record but does not count it as a request, usage or cost. With 241,790 uncached input tokens, the token-only API-equivalent comparison estimate is `(241,790 × $4.00 + 9,277,952 × $0.40 + 73,706 × $20.00) / 1,000,000 = $6.1524608`, rounded to $6.15. The measurement includes only the root Codex Desktop task in the selected session; no subagent was created, and separate tasks, reused threads, hosted review and unmeasured external execution are excluded. No product-code change, physical-device operation or FTMS Control Point write is included. The final numeric insertion, post-insertion mechanical checks, any later commit and any push are excluded.

`PP-20260903-05` covers the authorised documentation-only GitHub issue #4 source review, safety-gated workout execution state-machine design, review-driven Mermaid diagrams, documentation checks, repository skill validation, focused and complete simulator tests, simulator build, static analysis, full diff review and final commit-authorisation preparation. The exact phase-start snapshot in session `01a066e7-e923-73e2-87fc-4d5a62d8b7dc` reported 134,782 input tokens, of which 113,792 were cached, 0 cache-write input tokens, and 843 output tokens, including 261 reasoning tokens, for 135,625 total tokens. The final pre-commit ledger snapshot reported 8,651,307 input tokens, of which 8,244,224 were cached, 0 cache-write input tokens, and 44,728 output tokens, including 19,521 reasoning tokens, for 8,696,035 total tokens. Subtracting the start snapshot produced 8,516,525 input tokens, of which 8,130,432 were cached, 0 cache-write input tokens, and 43,885 output tokens, including 19,260 reasoning tokens. The total phase delta was 8,560,410 tokens. None of the 59 measured documentation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 215,213 tokens. With 386,093 uncached input tokens, the token-only API-equivalent comparison estimate is `(386,093 × $4.00 + 8,130,432 × $0.40 + 43,885 × $20.00) / 1,000,000 = $5.6742448`, rounded to $5.67. This exact boundary includes the first numeric ledger insertion, its mechanical checks, the initial handoff/status review, the requested diagram revision and the user's later commit, push and issue-close authorisation; it includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and external tool/provider execution are excluded. No executable state machine, product-code change, FTMS Control Point write, treadmill operation, iPhone installation or physical-device validation is included. This replacement numeric insertion, post-insertion mechanical checks, commit, push and issue closure are excluded.

`PP-20260903-06` covers the authorised GitHub issue #9 attachment review, unchanged `2c-ceiling` export installation, single-size `AppIcon` configuration, asset validation, complete simulator tests, simulator build, static analysis, iPhone simulator Home Screen and Search review, explicit Default, Dark and Tinted treatment review, and full diff review. The exact phase-start snapshot in session `01a0670c-4f9b-7da1-bf0f-971fd2a79e70` reported 96,139 input tokens, of which 78,720 were cached, 0 cache-write input tokens, and 940 output tokens, including 372 reasoning tokens, for 97,079 total tokens. The phase-report helper rejected token-count event 44 because it repeated an unchanged cumulative snapshot while retaining non-zero metered `last_token_usage` counters. In accordance with the repository accounting skill, no end snapshot, phase delta, threshold result or API-equivalent cost was reconstructed from the malformed session data. The boundary includes only the root Codex Desktop task; no subagent was created, and separate tasks, hosted review and external tool/provider execution are excluded. No application behaviour, FTMS or Bluetooth code, treadmill operation, physical-device validation, commit, push or issue maintenance is included.

`PP-20260903-07` covers the authorised documentation-only GitHub issue #5 source and product-context review, local workout storage and history contract, README integration, non-duplicative follow-on issues #10 and #11, issue #6 dependency re-triage, documentation checks, focused and complete simulator tests, simulator build, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a06737-0477-7312-9184-13dc6e28dc2b` reported 99,370 input tokens, of which 80,000 were cached, 0 cache-write input tokens, and 1,068 output tokens, including 422 reasoning tokens, for 100,438 total tokens. The final pre-ledger snapshot reported 5,307,706 input tokens, of which 5,166,080 were cached, 0 cache-write input tokens, and 23,197 output tokens, including 10,009 reasoning tokens, for 5,330,903 total tokens. Subtracting the start snapshot produced 5,208,336 input tokens, of which 5,086,080 were cached, 0 cache-write input tokens, and 22,129 output tokens, including 9,587 reasoning tokens. The total phase delta was 5,230,465 tokens. None of the 53 measured documentation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 148,624 tokens. With 122,256 uncached input tokens, the token-only API-equivalent comparison estimate is `(122,256 × $4.00 + 5,086,080 × $0.40 + 22,129 × $20.00) / 1,000,000 = $2.9660360`, rounded to $2.97. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external tool execution are excluded. No persistence implementation, SwiftUI product-flow change, network or OpenRouter work, API-key UI, FTMS write, workout execution, HealthKit, watchOS, hardware operation or physical-device validation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue closure are excluded.

`PP-20260903-08` covers the authorised GitHub issue #10 source and issue review, Foundation-only versioned saved-plan repository, synthetic lifecycle and failure-injection tests, README integration, focused and complete simulator tests, simulator build, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a06747-62ef-71d2-b08a-9b28df59224a` reported 97,716 input tokens, of which 79,744 were cached, 0 cache-write input tokens, and 700 output tokens, including 218 reasoning tokens, for 98,416 total tokens. The final pre-ledger snapshot reported 5,771,574 input tokens, of which 5,590,144 were cached, 0 cache-write input tokens, and 39,202 output tokens, including 17,501 reasoning tokens, for 5,810,776 total tokens. Subtracting the start snapshot produced 5,673,858 input tokens, of which 5,510,400 were cached, 0 cache-write input tokens, and 38,502 output tokens, including 17,283 reasoning tokens. The total phase delta was 5,712,360 tokens. None of the 49 measured implementation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 188,800 tokens. With 163,458 uncached input tokens, the token-only API-equivalent comparison estimate is `(163,458 × $4.00 + 5,510,400 × $0.40 + 38,502 × $20.00) / 1,000,000 = $3.6280320`, rounded to $3.63. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external execution are excluded. The initial sandboxed simulator attempt could not access CoreSimulator or DerivedData and is not counted as validation evidence; the successful rerun used the same focused command with the required access. No Plans or History UI, manual plan flow, network or OpenRouter work, API-key UI, execution history, FTMS write, workout execution, HealthKit, watchOS, hardware operation or physical-device validation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue maintenance are excluded.

`PP-20260903-09` covers the authorised GitHub issue #11 source and dependency review, repository-backed saved-plan list, manual walking/running plan entry, localised input boundary, current-capability adaptation, deterministic validation, exact preview and derived totals, confirmation-only create and identity-preserving edit flow, explicit repository failure presentation, synthetic presentation and XCUITests, README integration, focused and complete simulator tests, simulator build, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a0677f-fe03-7d41-ad6a-c4be92a9b7eb` reported 96,853 input tokens, of which 78,592 were cached, 0 cache-write input tokens, and 1,026 output tokens, including 131 reasoning tokens, for 97,879 total tokens. The final pre-ledger snapshot reported 20,682,346 input tokens, of which 20,428,672 were cached, 0 cache-write input tokens, and 62,997 output tokens, including 16,333 reasoning tokens, for 20,745,343 total tokens. Subtracting the start snapshot produced 20,585,493 input tokens, of which 20,350,080 were cached, 0 cache-write input tokens, and 61,971 output tokens, including 16,202 reasoning tokens. The total phase delta was 20,647,464 tokens. None of the 126 measured implementation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 224,572 tokens. With 235,413 uncached input tokens, the token-only API-equivalent comparison estimate is `(235,413 × $4.00 + 20,350,080 × $0.40 + 61,971 × $20.00) / 1,000,000 = $10.3211040`, rounded to $10.32. The focused presentation run covered 15 Plans and 17 treadmill-capability tests; the complete simulator suite passed all 81 unit and UI tests, including six multi-launch XCUITests. The initial sandboxed build could not access CoreSimulator and is not validation evidence; the subsequent access-enabled focused tests, complete suite, build and analysis are the reported evidence. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external execution are excluded. No plan deletion or recovery UI, history, network or OpenRouter work, API-key UI, FTMS Control Point write, workout execution, HealthKit, watchOS, hardware operation, iPhone installation or physical-device validation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue maintenance are excluded.

`PP-20260903-10` covers the authorised documentation-only GitHub issue #6 authority and accepted-dependency review, provider-neutral workout-proposal semantics and outcomes, deterministic mapping and validation gates, independent lifecycle states, production privacy/disclosure and credential-reset decisions, developer-only evaluation boundary, shared deterministic evaluation method, operational evidence and provenance requirements, regression and decision ratification rules, README integration, focused and complete simulator tests, simulator build, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a0685c-3a19-7832-a8d1-1ab0c86d6a87` reported 65,515 input tokens, of which 46,848 were cached, 0 cache-write input tokens, and 699 output tokens, including 346 reasoning tokens, for 66,214 total tokens. The final pre-ledger snapshot reported 4,858,456 input tokens, of which 4,718,720 were cached, 0 cache-write input tokens, and 18,194 output tokens, including 6,846 reasoning tokens, for 4,876,650 total tokens. Subtracting the start snapshot produced 4,792,941 input tokens, of which 4,671,872 were cached, 0 cache-write input tokens, and 17,495 output tokens, including 6,500 reasoning tokens. The total phase delta was 4,810,436 tokens. None of the 48 measured documentation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 147,486 tokens. With 121,069 uncached input tokens, the token-only API-equivalent comparison estimate is `(121,069 × $4.00 + 4,671,872 × $0.40 + 17,495 × $20.00) / 1,000,000 = $2.7029248`, rounded to $2.70. The focused plan-domain and presentation run passed all 25 tests; the complete simulator suite passed all 81 unit and UI tests with no failures or skips. Simulator build and static analysis passed, as did all 18 phase-accounting helper tests and all three repository skill validators. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external execution are excluded. No inference implementation, evaluation corpus, schema file, scorer, runner, evaluation target, network call, API-key UI, Keychain item, Apple Foundation Models or OpenRouter integration, persistence change, export implementation, FTMS write, workout execution, HealthKit, watchOS, hardware operation, iPhone installation or physical-device validation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue maintenance are excluded.

`PP-20260903-11` covers the authorised GitHub issue #12 authority and accepted-dependency review, versioned provider-neutral proposal and normalized-result schemas, 20-case synthetic workout-import corpus and canonical hash, standard-library-only schema validation and deterministic scorer, complete and deliberately failing normalized fixtures, ignored local raw-run boundary, documentation, focused tests, complete simulator tests, simulator build, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a06890-1812-7b30-90c9-5a60a0ee3486` reported 534,488 input tokens, of which 484,992 were cached, 0 cache-write input tokens, and 3,508 output tokens, including 1,183 reasoning tokens, for 537,996 total tokens. The final pre-ledger snapshot reported 12,199,253 input tokens, of which 11,908,864 were cached, 0 cache-write input tokens, and 71,416 output tokens, including 20,747 reasoning tokens, for 12,270,669 total tokens. Subtracting the start snapshot produced 11,664,765 input tokens, of which 11,423,872 were cached, 0 cache-write input tokens, and 67,908 output tokens, including 19,564 reasoning tokens. The total phase delta was 11,732,673 tokens. None of the 85 measured implementation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 240,426 tokens. One unchanged cumulative snapshot after context maintenance reported zero metered request components plus a standalone 24,075-token context-size value; the helper reports that record but does not count it as a request, usage or cost. With 240,893 uncached input tokens, the token-only API-equivalent comparison estimate is `(240,893 × $4.00 + 11,423,872 × $0.40 + 67,908 × $20.00) / 1,000,000 = $6.8912808`, rounded to $6.89. The focused workout-import suite passed all 30 tests; the fixed normalized fixture passed all 20 corpus cases, whose canonical hash is `be6355a1afdd9d56a14c759e3da0ef836280091218a6f282d7a826029cade79c`. The complete simulator suite passed all 81 unit and UI tests with no failures or skips. Simulator build and static analysis passed, as did all 18 phase-accounting helper tests and all three repository skill validators. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external execution are excluded. No provider adapter, provider SDK, runner, network call, API-key handling, evaluation UI, model run or comparison, decision parameter, production inference, persistence change, FTMS write, workout execution, HealthKit, watchOS, hardware operation, iPhone installation or physical-device validation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue maintenance are excluded.

`PP-20260903-12` covers the authorised GitHub issue #13 authority and accepted-dependency review, isolated developer-only `PacePromptEvaluation` and `PacePromptEvaluationTests` targets, provider-neutral corpus runner and normalized result envelope, exact canonical mapping through the unchanged local validator, Apple Foundation Models guided-generation adapter, strict-schema OpenRouter adapter with redacted process-only credentials and separately supplied request authorisation, readiness-only evaluation UI, ignored raw-run writer, deterministic fixture/fake tests, documentation, production-target and archive isolation, leakage checks, focused and complete simulator tests, simulator and generic physical-iPhone configuration builds, static analysis, phase-helper tests, repository skill validation and full diff review. The exact phase-start snapshot in session `01a068be-55b6-76f1-af25-f138ce50e5b1` reported 474,030 input tokens, of which 435,200 were cached, 0 cache-write input tokens, and 2,656 output tokens, including 1,039 reasoning tokens, for 476,686 total tokens. The final pre-ledger snapshot reported 16,562,457 input tokens, of which 16,188,544 were cached, 0 cache-write input tokens, and 86,369 output tokens, including 29,662 reasoning tokens, for 16,648,826 total tokens. Subtracting the start snapshot produced 16,088,427 input tokens, of which 15,753,344 were cached, 0 cache-write input tokens, and 83,713 output tokens, including 28,623 reasoning tokens. The total phase delta was 16,172,140 tokens. None of the 123 measured implementation-phase requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 242,480 tokens. One unchanged cumulative snapshot after context maintenance reported zero metered request components plus a standalone 25,018-token context-size value; the helper reports that record but does not count it as a request, usage or cost. With 335,083 uncached input tokens, the token-only API-equivalent comparison estimate is `(335,083 × $4.00 + 15,753,344 × $0.40 + 83,713 × $20.00) / 1,000,000 = $9.3159296`, rounded to $9.32. The focused evaluation suite passed all 14 tests; the unchanged scorer passed all 30 tests, and its fixed normalized fixture passed all 20 corpus cases at hash `be6355a1afdd9d56a14c759e3da0ef836280091218a6f282d7a826029cade79c`. The complete production simulator suite passed all 81 unit and UI tests with no failures or skips. Both simulator builds, the unsigned generic physical-iPhone configuration build, both static analyses and the unsigned production archive passed, as did all 18 phase-accounting helper tests and all three repository skill validators. The production archive contained no evaluation resources, OpenRouter or Foundation Models linkage, and no credential or raw-run artifact was tracked. This exact boundary includes only the root Codex Desktop task in the explicitly selected session. No subagent was created, and separate tasks, hosted review and unmeasured external execution are excluded. No real provider call, physical-iPhone inference, model comparison, model set, repetition choice, spending limit, comparison rubric, hard safety gate, fallback policy, provider decision, production inference, user-prompt path, Keychain UI, persistence change, automatic publication, FTMS Control Point write, workout execution, HealthKit, watchOS, treadmill operation or iPhone installation is included. The final numeric insertion, post-insertion mechanical checks, commit, push and issue maintenance are excluded.

`PP-20260904-01` covers the authorised GitHub issue #15 design ratification, isolated host-side prompt-v2 evaluation harness, separately versioned development examples and held-out cases, provider-profile strategy implementation, deterministic scorer integration, mocked outbound payload and guard validation, measured OpenRouter evaluation, evidence audit, provider-decision recording, accounting-helper replay correction, focused tests and full diff review. No phase-start snapshot was captured, so this is explicitly a whole-session aggregate for session `01a06b75-7d90-7332-9949-a84ca6e5df65`, not a per-commit delta. The final pre-ledger snapshot reported 112,965,849 input tokens, of which 111,188,352 were cached, 0 cache-write input tokens, and 486,356 output tokens, including 168,068 reasoning tokens, for 113,452,205 total tokens. None of the 862 measured Codex requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 221,792 tokens. Eight unchanged cumulative snapshots after context maintenance had zero metered request components and are reported but not counted as requests, usage or cost. Four token events were exact canonical `info`-payload replays of their immediately preceding events; the corrected helper reports those replay pairs separately and excludes them from the request count, usage reconciliation and cost. A synthetic lookalike with any changed `info` field and non-zero metered counters remains an actionable error. With 1,777,497 uncached input tokens, the token-only API-equivalent comparison estimate is `(1,777,497 × $4.00 + 111,188,352 × $0.40 + 486,356 × $20.00) / 1,000,000 = $61.3124488`, rounded to $61.31. Reasoning tokens are already a subset of output and are not added or priced again. The separate OpenRouter evaluation reported 665,165 prompt tokens and 34,415 completion tokens, for 699,580 provider tokens and $0.95952008; those provider counters and spend are not included in the Codex totals or API-equivalent estimate. The measurement includes only the root Codex Desktop task in the explicitly selected session; no subagent was created, and separate tasks, hosted review and other execution channels are excluded. No production provider was selected. No production inference, Apple Foundation Models, iPhone access, HealthKit, watchOS, treadmill connection, FTMS Control Point write or workout execution is included. The final numeric insertion, post-insertion mechanical checks, any later commit, push, GitHub update, issue closure or result publication are excluded.

`PP-20260904-02` covers the authorised prompt-only GitHub issue #15 slice: recall of the ratified v3 contract, the ratification and base-dependency records, the frozen v2.9 prompt and unchanged model-output schema, drafting `Evaluation/WorkoutImport/HostEval/prompts/v3/system.md` with the ratified rules 4, 5 and 7, the mechanical v3 path convention and eleven development examples, the justified line-by-line diff against v2.9, ratification, the follow-on evaluation-policy decisions recorded on the issue (category-floor denominator, composite and selection rules, p95 latency definition, tie handling and frozen generation settings), this accounting note and the pre-commit checks. It is measured from Claude Code session `b409f3ae-2946-4281-963d-7d29f3d8ec17`, file `b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl`, with `claude-fable-5-1` on every assistant record, from the first user message (uuid `11c97315-ed7f-4424-b7d9-d6ec2c3af7e9`, `2026-09-04T22:43:58.707Z`) through the phase-end assistant message (uuid `c21ac956-c70c-42e6-afef-d7dd54eb57a6`, `2026-09-04T23:02:14.883Z`, message id `msg_011CejEPkZYzLCjzKbxmu2qZ`). 62 assistant JSONL lines deduplicate to 20 API responses by `message.id`, and every duplicate line repeats identical usage. The exact delta is 400 uncached input tokens, 1,602,944 cache-read input tokens, 0 five-minute cache-write tokens and 111,519 one-hour cache-write tokens, for 1,714,863 total input tokens, plus 35,153 output tokens, including 16,148 thinking tokens, for 1,750,016 total tokens. The token-only API-equivalent comparison estimate is `(400 × $10.00 + 1,602,944 × $0.25 + 0 × $12.50 + 111,519 × $20.00 + 35,153 × $50.00) / 1,000,000 = $4.392766`, rounded to $4.39. This is a Claude Code subscription session and not an API bill. No subagent was used. No provider call, `.runs` content, dataset, manifest, schema, run policy, models file, README, `task.py`, test or `prompts/v2` file was touched, and no push, GitHub comment or corpus authoring is included. The assistant messages that insert this row and perform the commit follow the phase-end boundary and are excluded. Reproducible summation from the session file:

```shell
jq -s --arg start "2026-09-04T22:43:58.707Z" --arg end "c21ac956-c70c-42e6-afef-d7dd54eb57a6" '[.[] | select(.type=="assistant" and .timestamp >= $start)] as $a | ($a | map(.uuid) | index($end)) as $i | ($a[:$i+1] | unique_by(.message.id)) as $m | {messages: ($m|length), uncached_input: ($m|map(.message.usage.input_tokens)|add), cache_read: ($m|map(.message.usage.cache_read_input_tokens)|add), cache_write_5m: ($m|map(.message.usage.cache_creation.ephemeral_5m_input_tokens)|add), cache_write_1h: ($m|map(.message.usage.cache_creation.ephemeral_1h_input_tokens)|add), output: ($m|map(.message.usage.output_tokens)|add), thinking: ($m|map(.message.usage.output_tokens_details.thinking_tokens)|add)}' ~/.claude/projects/-Users-sertanyamaner-git-PacePrompt/b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl
```

`PP-20260905-01` covers the authorised GitHub issue #15 corpus slice on top of the frozen v3 prompt: reading the v2 dataset, manifest, verifier, scorer-adapter and v1 scorer contracts, authoring the 20 development cases (11 ordered few-shot examples plus 9 development-only cases including the fixed warm-up) and 79 held-out cases with per-case rationales citing v3 rule numbers, both manifests with authoring blocks, the non-duplication review naming each held-out case's nearest development case, mechanical verification (schema validation of every expected output, unchanged v1 scorer mapping and local validation of every proposal, path ordering, uniqueness, skeleton collision and wording checks), the operator amendments (H063 relabelled to `steps`, H065 replaced by the missing-activity case, H012 and H030 ratified), sealing, this accounting note and the pre-commit checks. It is measured from Claude Code session `b409f3ae-2946-4281-963d-7d29f3d8ec17`, file `b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl`, with `claude-fable-5-1` on every assistant record, from the corpus-authorisation user message (uuid `9e1f6726-3a34-4171-b82c-a6d304f261f5`, `2026-09-04T23:06:53.153Z`) through the phase-end assistant message (uuid `7ce526f2-1e2e-4277-8d8a-00396a9116b2`, `2026-09-04T23:28:03.163Z`, message id `msg_011CejGNq19sDLLGoahi3uoL`). 69 assistant JSONL lines deduplicate to 17 API responses by `message.id`, and every duplicate line repeats identical usage. The exact delta is 424 uncached input tokens, 4,371,319 cache-read input tokens, 0 five-minute cache-write tokens and 138,763 one-hour cache-write tokens, for 4,510,506 total input tokens, plus 94,606 output tokens, including 33,030 thinking tokens, for 4,605,112 total tokens. The token-only API-equivalent comparison estimate is `(424 × $10.00 + 4,371,319 × $0.25 + 0 × $12.50 + 138,763 × $20.00 + 94,606 × $50.00) / 1,000,000 = $8.602630`, rounded to $8.60. This is a Claude Code subscription session and not an API bill. The 4 API responses between the `PP-20260904-02` phase-end message and this phase's start (the earlier row insertion, commit, wrap-up and evaluation-policy discussion) total 671,068 tokens and are attributed to neither row; they are reported here as an unmeasured tail rather than inferred into either delta. No subagent was used. No provider call, `.runs` content, harness code, schema, run policy, models file, README, `task.py`, test or `prompts/` file was touched, and no push or GitHub comment is included. The assistant messages that insert this row and perform the commit follow the phase-end boundary and are excluded. Reproducible summation from the session file:

```shell
jq -s --arg start "2026-09-04T23:06:53.153Z" --arg end "7ce526f2-1e2e-4277-8d8a-00396a9116b2" '[.[] | select(.type=="assistant" and .timestamp >= $start)] as $a | ($a | map(.uuid) | index($end)) as $i | ($a[:$i+1] | unique_by(.message.id)) as $m | {messages: ($m|length), uncached_input: ($m|map(.message.usage.input_tokens)|add), cache_read: ($m|map(.message.usage.cache_read_input_tokens)|add), cache_write_5m: ($m|map(.message.usage.cache_creation.ephemeral_5m_input_tokens)|add), cache_write_1h: ($m|map(.message.usage.cache_creation.ephemeral_1h_input_tokens)|add), output: ($m|map(.message.usage.output_tokens)|add), thinking: ($m|map(.message.usage.output_tokens_details.thinking_tokens)|add)}' ~/.claude/projects/-Users-sertanyamaner-git-PacePrompt/b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl
```

`PP-20260905-02` covers the operator-identified correction to the sealed v3 corpus: held-out case `WI-V3-H064` previously ended its recursive sequence with heart-rate wording, which is an intensity-like unsupported target and would fire prompt rule 8 before the intended rule 10, so the prompt was rewritten as a purely recursive, unbounded sequence in supported units with no earlier-rule defect while the oracle stayed `excessiveComplexity` with `["steps"]`. The amendment is recorded in both manifest authoring blocks and the non-duplication review entry, the held-out manifest hash and review evidence were refreshed, and the full mechanical battery was rerun. It is measured from Claude Code session `b409f3ae-2946-4281-963d-7d29f3d8ec17`, file `b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl`, with `claude-fable-5-1` on every assistant record, from the correction user message (uuid `655633e3-33a8-42ee-8214-5edc055ed3d7`, `2026-09-04T23:35:24.777Z`) through the phase-end assistant message (uuid `955c643d-16d5-418b-aa1a-d3ca3a35b65a`, `2026-09-04T23:36:10.642Z`, message id `msg_011CejGydpRxDmpGQnHXSTU7`). 5 assistant JSONL lines deduplicate to 1 API responses by `message.id`, and every duplicate line repeats identical usage. The exact delta is 2 uncached input tokens, 325,757 cache-read input tokens, 0 five-minute cache-write tokens and 210 one-hour cache-write tokens, for 325,969 total input tokens, plus 3,529 output tokens, including 1,695 thinking tokens, for 329,498 total tokens. The token-only API-equivalent comparison estimate is `(2 × $10.00 + 325,757 × $0.25 + 0 × $12.50 + 210 × $20.00 + 3,529 × $50.00) / 1,000,000 = $0.262109`, rounded to $0.26. This is a Claude Code subscription session and not an API bill. The 3 API responses between the `PP-20260905-01` phase-end message and this phase's start (that row's insertion, commit and wrap-up) total 968,654 tokens and are reported as an unmeasured tail attributed to neither row. No subagent was used; no provider call, `.runs` content, harness code, schema, policy, models file, README, `task.py`, test or `prompts/` file was touched; no push or GitHub comment is included. The assistant messages that insert this row and perform the commit follow the phase-end boundary and are excluded. Reproducible summation from the session file:

```shell
jq -s --arg start "2026-09-04T23:35:24.777Z" --arg end "955c643d-16d5-418b-aa1a-d3ca3a35b65a" '[.[] | select(.type=="assistant" and .timestamp >= $start)] as $a | ($a | map(.uuid) | index($end)) as $i | ($a[:$i+1] | unique_by(.message.id)) as $m | {messages: ($m|length), uncached_input: ($m|map(.message.usage.input_tokens)|add), cache_read: ($m|map(.message.usage.cache_read_input_tokens)|add), cache_write_5m: ($m|map(.message.usage.cache_creation.ephemeral_5m_input_tokens)|add), cache_write_1h: ($m|map(.message.usage.cache_creation.ephemeral_1h_input_tokens)|add), output: ($m|map(.message.usage.output_tokens)|add), thinking: ($m|map(.message.usage.output_tokens_details.thinking_tokens)|add)}' ~/.claude/projects/-Users-sertanyamaner-git-PacePrompt/b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl
```

`PP-20260905-03` covers the operator-approved revision of the sealed v3 corpus: held-out cases `WI-V3-H001`, `H046`, `H055`, `H060`, `H070` and `H077` rewritten with materially different semantic structures from their nearest development cases (labelled lines; a whole-workout gradient stated once in degrees; a scoped speed over a repeated block; a spelled-out count over a three-step set; a unit choice delegated to the model in a separate sentence; a scoped per-step duration contradicted by one step) with categories, paths and proposal roles unchanged; explicit interval and recovery kinds added to `H055`; the review's nearest-development comparisons rewritten with substantive structural distinctions; the two review few-shot flags for `H046` and `H060` corrected to false, with the flag now derived mechanically from the referenced development case and asserted across all 79 entries; both manifest amendment records, the held-out manifest hash and the review evidence refreshed; and the complete schema, scorer, validator, composition and non-duplication battery rerun. It is measured from Claude Code session `b409f3ae-2946-4281-963d-7d29f3d8ec17`, file `b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl`, with `claude-fable-5-1` on every assistant record, from the operator's restructuring approval (uuid `3dcc577c-51c1-4b9f-9fce-1a1379856dd1`, `2026-09-04T23:40:42.236Z`) through the phase-end assistant message (uuid `e392ac5e-2f13-4b1e-8df9-53724073539b`, `2026-09-04T23:45:56.771Z`, message id `msg_011CejHmV7qHJC7ZkzevHaH7`). 21 assistant JSONL lines deduplicate to 5 API responses by `message.id`, and every duplicate line repeats identical usage. The exact delta is 70 uncached input tokens, 1,743,820 cache-read input tokens, 0 five-minute cache-write tokens and 20,879 one-hour cache-write tokens, for 1,764,769 total input tokens, plus 16,449 output tokens, including 4,253 thinking tokens, for 1,781,218 total tokens. The token-only API-equivalent comparison estimate is `(70 × $10.00 + 1,743,820 × $0.25 + 0 × $12.50 + 20,879 × $20.00 + 16,449 × $50.00) / 1,000,000 = $1.676685`, rounded to $1.68. This is a Claude Code subscription session and not an API bill. The 3 API responses between the `PP-20260905-02` phase-end message and this phase's start (that row's insertion, commit and wrap-up, and a status question) total 1,011,945 tokens and are reported as an unmeasured tail attributed to neither row. No subagent was used; no provider call, `.runs` content, harness code, schema, policy, models file, README, `task.py`, test or `prompts/` file was touched; no push or GitHub comment is included. The assistant messages that insert this row and perform the commit follow the phase-end boundary and are excluded. Reproducible summation from the session file:

```shell
jq -s --arg start "2026-09-04T23:40:42.236Z" --arg end "e392ac5e-2f13-4b1e-8df9-53724073539b" '[.[] | select(.type=="assistant" and .timestamp >= $start)] as $a | ($a | map(.uuid) | index($end)) as $i | ($a[:$i+1] | unique_by(.message.id)) as $m | {messages: ($m|length), uncached_input: ($m|map(.message.usage.input_tokens)|add), cache_read: ($m|map(.message.usage.cache_read_input_tokens)|add), cache_write_5m: ($m|map(.message.usage.cache_creation.ephemeral_5m_input_tokens)|add), cache_write_1h: ($m|map(.message.usage.cache_creation.ephemeral_1h_input_tokens)|add), output: ($m|map(.message.usage.output_tokens)|add), thinking: ($m|map(.message.usage.output_tokens_details.thinking_tokens)|add)}' ~/.claude/projects/-Users-sertanyamaner-git-PacePrompt/b409f3ae-2946-4281-963d-7d29f3d8ec17.jsonl
```
`PP-20260905-04` covers the authorised continuation of GitHub issue #15 after `PP-20260904-01`: prompt-v3 and corpus review, final policy ratification, an isolated sibling implementation of the confirmatory host-side verifier, deterministic 711-attempt queue, three per-model transport warm-ups, route-strategy registry reuse, unchanged-scorer compatibility projection, exact-rational aggregation, category attempt and majority floors, development-host latency and cost tie-breaks, cancellation and rate-limit preservation, calibrated whole-run preflight, strict runtime spending guard, mocked outbound payload inspection, read-only catalogue refreshes, zero-spend operator-gate preparation, focused and complete host tests, legacy-v2 verification and full diff review. The exact phase starts at token-count event 874 in session `01a06b75-7d90-7332-9949-a84ca6e5df65`, timestamp `2026-09-04T21:46:31.012Z`, with 112,965,849 input tokens, of which 111,188,352 were cached, 0 cache-write input tokens, and 486,356 output tokens, including 168,068 reasoning tokens, for 113,452,205 total tokens. The final pre-ledger snapshot at event 1220, timestamp `2026-09-05T08:12:15.419Z`, reported 157,732,312 input tokens, of which 154,855,424 were cached, 0 cache-write input tokens, and 682,416 output tokens, including 250,381 reasoning tokens, for 158,414,728 total tokens. Subtracting the start produced 44,766,463 input tokens, of which 43,667,072 were cached, 0 cache-write input tokens, and 196,060 output tokens, including 82,313 reasoning tokens. The total phase delta was 44,962,523 tokens. None of the 340 measured requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 243,374 tokens. Three unchanged cumulative snapshots after context maintenance reported zero metered request components plus standalone context-size values and are not counted as requests, usage or cost. Three exact canonical token-event replays are likewise reported and excluded. With 1,099,391 uncached input tokens, the token-only API-equivalent comparison estimate is `(1,099,391 × $4.00 + 43,667,072 × $0.40 + 196,060 × $20.00) / 1,000,000 = $25.7855928`, rounded to $25.79, using the GPT-5.6 Sol rates rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. The reproducible report is `/private/tmp/paceprompt-issue15-v3-pp04-final-report.json`, generated from `/Users/sertanyamaner/.codex/sessions/2026/09/04/rollout-2026-09-04T09-07-37-01a06b75-7d90-7332-9949-a84ca6e5df65.jsonl` with baseline `/private/tmp/paceprompt-issue15-v3-pp04-baseline.json`. This measurement includes only the root Codex Desktop task in the selected session. Separately measured Fable sessions, separate tasks, hosted review and external provider execution are excluded; no subagent was created. The catalogue and mock gates made zero model-provider calls, read no credential and spent $0.00. No live v3 result, production inference, provider selection, iPhone or Apple Foundation Models access, treadmill or FTMS activity, workout execution, HealthKit or watchOS work is included. This final numeric insertion, post-insertion mechanical checks, any later commit, integration merge, push, live provider call, issue closure or result publication are excluded.

`PP-20260905-05` covers the operator-authorised merge-based integration of the ratified prompt-v3 and sealed corpus branch at `dcfdd4798d87b58dfb1b95ade9f257784f236f4a` with the sibling host-harness commit `63b612ddd43da5dd280f049585778d5f1d7e38df`, without rebasing either history. The only merge conflict was the additive `DEVELOPMENT_NOTES.md` ledger, resolved by retaining all four Fable-authored rows and narratives and the Codex harness row unchanged before adding this separate integration row. The integrated tree passed 62 host tests, legacy and v3 `verify`, Python compilation and staged diff checks; the frozen prompt hash `d58800efc4b0e01994a1a5be1a1d644ce52dbb6bb7c12655745dc78b6e355fd3` and all five sealed corpus hashes remained byte-identical. The integrated deterministic queue contains 711 scored calls and three warm-ups. The exact phase starts at token-count event 1241 in session `01a06b75-7d90-7332-9949-a84ca6e5df65`, timestamp `2026-09-05T08:40:10.245Z`, with 161,644,846 input tokens, of which 158,735,616 were cached, 0 cache-write input tokens, and 692,893 output tokens, including 254,453 reasoning tokens, for 162,337,739 total tokens. The final pre-ledger snapshot at event 1251, timestamp `2026-09-05T08:41:17.210Z`, reported 162,314,534 input tokens, of which 159,339,648 were cached, 0 cache-write input tokens, and 695,109 output tokens, including 254,931 reasoning tokens, for 163,009,643 total tokens. Subtracting the start produced 669,688 input tokens, of which 604,032 were cached, 0 cache-write input tokens, and 2,216 output tokens, including 478 reasoning tokens. The total phase delta was 671,904 tokens. None of the 10 measured requests crossed the ledger-authorised 272,000-token threshold; the largest request input was 80,878 tokens. With 65,656 uncached input tokens, the token-only API-equivalent comparison estimate is `(65,656 × $4.00 + 604,032 × $0.40 + 2,216 × $20.00) / 1,000,000 = $0.5485568`, rounded to $0.55, using the GPT-5.6 Sol rates rechecked on 3 September 2026. Reasoning remains a subset of output and is not added or priced again. The reproducible report is `/private/tmp/paceprompt-issue15-v3-integration-final-report.json`, generated from the selected session with baseline `/private/tmp/paceprompt-issue15-v3-integration-baseline.json`. This measurement includes only the root Codex Desktop task in the selected session. The earlier `PP-20260905-04` phase, separately measured Fable sessions, separate tasks, hosted review and external provider execution are excluded; no subagent was created. Integration made zero provider calls, read no credential and spent $0.00. No push, live v3 call, provider selection, result publication, issue closure, iPhone or Apple Foundation Models access, treadmill or FTMS activity, workout execution, HealthKit or watchOS work is included. This numeric insertion, post-insertion checks, merge commit and any later action are excluded.
